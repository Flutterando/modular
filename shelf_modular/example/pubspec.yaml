name: shelf_modular_example
description: A web server built using the shelf package.
version: 1.0.0
publish_to: none
# homepage: https://www.example.com

resolution: workspace

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  hotreloader: ^4.3.0
  shelf: ^1.3.0
  fpdart: ^0.0.10
  postgres: ^3.5.6
  jose: ^0.3.2
  redis_dart: ^0.1.0+4
  uuid: ^4.5.1
  shelf_modular:

dev_dependencies:
  pedantic: ^1.10.0
  test: ">=1.16.0 <2.0.0"
