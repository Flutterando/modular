// GENERATED by bin/build_index.dart — do not edit by hand.
// Regenerate with: dart run bin/build_index.dart
// ignore_for_file: type=lint

import '../doc_chunk.dart';

const List<DocPage> docPages = [
  DocPage(
    path: "flutter_modular/dependency-injection.md",
    title: "Dependency injection",
    markdown: "# Dependency injection\n\nModular's DI is powered by [`auto_injector`](https://pub.dev/packages/auto_injector).\nYou register constructors on a module's `ModularContext`; Modular resolves each\ndependency's own constructor arguments from the graph automatically.\n\n## Registering dependencies\n\nInside `register`, declare dependencies on the context (`c`):\n\n```dart\nfinal coreModule = createModule(\n  register: (c) {\n    c\n      ..addSingleton<ProductService>(ProductService.new)\n      ..addSingleton<ProductRepository>(ProductRepository.new) // gets ProductService injected\n      ..addSingleton<AppSession>(AppSession.new);\n  },\n);\n```\n\n| Method | Builds | Lifetime |\n|---|---|---|\n| `add<T>(T Function(...))` | a **new** instance every time it is resolved | per resolution |\n| `addSingleton<T>(T Function(...))` | once, **eagerly** at registration | one shared instance |\n| `addLazySingleton<T>(T Function(...))` | once, on **first** resolution | one shared instance |\n| `addInstance<T>(T value)` | nothing — registers an existing object | the value you pass |\n\nYou pass a **constructor reference** (e.g. `ProductRepository.new`), not an instance.\n`auto_injector` reads its parameters and supplies them from the graph, so a repository\nthat takes a service in its constructor just works once both are registered.\n\n## Resolving with `inject<T>()`\n\nConstructor injection covers most needs — a class declares what it needs as constructor\nparameters and the graph fills them in. When you need to reach a dependency from a place\nthat has no constructor — a route **guard**, a callback, a top‑level function — use\n`inject<T>()`:\n\n```dart\nc.route(\n  '/settings/secret',\n  // inject<T>() reads DI at guard-eval time without exposing the injector object.\n  guards: [(state) => inject<AppSession>().unlocked ? null : '/home/settings'],\n  child: (ctx, state) => const SecretPage(),\n);\n```\n\n`inject<T>()` resolves `T` from the **live** module graph (the injector object itself\nstays private — Angular‑style). It throws a `StateError` if no Modular app has\nbootstrapped yet, and because it reads the live graph, a feature module's dependencies\nare reachable through it only while that feature is active.\n\n## Bind lifecycle\n\n*Where* a dependency is registered decides *how long* it lives — this is the other half\nof the module `path` rule from [Modules](./module.md#a-modules-path-decides-what-it-is).\n\n### Root‑owned (shared DI, never disposed)\n\nDependencies in a **path‑less** module are committed **eagerly** at bootstrap and live\nfor the whole app. They are your durable truth — repositories, services, sessions. A\nleaving route never disposes them.\n\n```dart\nfinal coreModule = createModule(            // no path → root-owned\n  register: (c) => c.addSingleton<ProductRepository>(ProductRepository.new),\n);\n```\n\n### Feature‑scoped (bound on entry, disposed on exit)\n\nDependencies in a module **with a `path`** are bound lazily when the feature's **first\nroute enters** the stack, and disposed when its **last route leaves**. Disposal is\nautomatic for `ChangeNotifier`s and [`Disposable`](./state-management.md#disposable)s —\ntheir `dispose()` is called.\n\n```dart\nfinal productsModule = createModule(\n  path: '/products',                        // feature → binds disposed when it leaves\n  register: (c) {\n    c.add<ProductSearchController>(ProductSearchController.new);\n    // ...routes...\n  },\n);\n```\n\nThis mirrors how the \"active path list\" worked in 6.x: a feature's resources exist only\nwhile the feature is on screen.\n\n:::tip Where should a dependency live?\nPut the **source of truth** (repositories, services, app session) in a root‑owned\nmodule — it must outlive any single page. Put **feature‑local** machinery in the\nfeature's own module so it is cleaned up when the user leaves. For state that is 1:1\nwith a single page, prefer page‑scoped [`provide`](./state-management.md) over a module\nbind.\n:::\n\n## Next\n\n- Bind state to a page's lifecycle → [State management](./state-management.md)\n- Use dependencies in guards → [Navigation › Guards](./navigation.md#guards)",
  ),
  DocPage(
    path: "flutter_modular/mcp-server.md",
    title: "Docs MCP server",
    markdown: "# Docs MCP server\n\n`flutter_modular_docs_mcp` is an [MCP](https://modelcontextprotocol.io) server that\nserves the **most up‑to‑date flutter_modular documentation** to your AI coding\nassistant. With it connected, your assistant searches and reads these real docs instead\nof guessing — it exposes a `search_docs` tool (keyword search) and a `read_doc` tool\n(fetch a full page).\n\n## Install\n\nYou need the [Dart SDK](https://dart.dev/get-dart). Activate the server from pub.dev:\n\n```bash\ndart pub global activate flutter_modular_docs_mcp\n```\n\nMake sure `~/.pub-cache/bin` is on your `PATH` so the `flutter_modular_docs_mcp` command\nresolves. Re‑run the same command anytime to update to the latest docs.\n\n## Register it in your assistant\n\nEvery MCP client has its own configuration, so instead of documenting each one, **paste\nthe prompt below into your agentic IDE or harness** (Claude Code, Cursor, Windsurf, VS\nCode, Zed, …) and let the agent install and register it for you:\n\n```text\nInstall and register the flutter_modular docs MCP server for me.\n\n1. Make sure the Dart SDK is available, then run:\n     dart pub global activate flutter_modular_docs_mcp\n   Ensure ~/.pub-cache/bin is on PATH so the `flutter_modular_docs_mcp` command resolves.\n\n2. Register it as a stdio MCP server in whatever client you are running in\n   (Claude Code, Cursor, Windsurf, VS Code, Zed, ...), with:\n     name:    flutter_modular_docs\n     command: flutter_modular_docs_mcp\n     type:    stdio\n   Use this client's own mechanism. For example, for Claude Code:\n     claude mcp add -s user flutter_modular_docs -- flutter_modular_docs_mcp\n   For other clients, edit their MCP config (e.g. mcp.json / settings) accordingly.\n\n3. Start a new session if the client requires it, then verify the server connects\n   and that the `search_docs` and `read_doc` tools are available.\n```\n\nOnce connected, ask your assistant anything about flutter_modular and it will ground its\nanswers in this documentation.",
  ),
  DocPage(
    path: "flutter_modular/migration.md",
    title: "Migrating from v6 to v7",
    markdown: "# Migrating from v6 to v7\n\n**flutter_modular 7 is a ground‑up rewrite.** The concepts you know — modules, routes,\ndependency injection — are all here, but the API changed shape: a module is now a small\nfunctional declaration, navigation moved onto `BuildContext`, and state is built into the\nroute lifecycle. This guide maps the old API to the new one.\n\n:::note\nLooking for **v5 → v6**? That guide is preserved under **(Legacy) Modular 6** in the\nsidebar.\n:::\n\n## At a glance\n\n| Area | v6 | v7 |\n|---|---|---|\n| Module | `class X extends Module { binds(i){} routes(r){} }` | `final x = createModule(register: (c) {...})` |\n| Child route | `r.child('/', child: (ctx) => P())` | `c.route('/', child: (ctx, state) => P())` |\n| Submodule | `r.module('/x', module: XModule())` | `c.module(xModule)` (module declares its own `path`) |\n| Redirect route | `RedirectRoute('/x', to: '/y')` | a [guard](./navigation.md#guards) returning `'/y'` |\n| Wildcard / 404 | `WildcardRoute(child: ...)` | a guard that redirects unknown paths |\n| Register a dep | `i.addSingleton(X.new)` in `binds` | `c.addSingleton<X>(X.new)` in `register` |\n| Resolve a dep | `Modular.get<X>()` | `inject<X>()` or constructor injection |\n| Navigate | `Modular.to.pushNamed('/x')` | `context.pushNamed('/x')` |\n| Replace stack | `Modular.to.navigate('/x')` | `context.navigate('/x')` |\n| Pop | `Modular.to.pop()` | `context.pop()` |\n| Route args | `Modular.args.data` / `args.params['id']` | `state.arguments` / `state['id']` |\n| Router config | `Modular.routerConfig` | `ModularApp.routerConfigOf(context)` |\n| Navigator key / observers / initial route | `Modular.setNavigatorKey/...` | `ModularApp(navigatorKey:, navigatorObservers:, initialRoute:)` |\n| Page state | bring‑your‑own | `route(provide:)` + `context.watch`/`read` |\n\n## Modules\n\nThe `binds`/`routes` method pair becomes a single `register` callback, and the module is\na `final` value rather than a class you instantiate.\n\n```dart title=\"v6\"\nclass HomeModule extends Module {\n  @override\n  void binds(i) {\n    i.addSingleton(HomeStore.new);\n  }\n\n  @override\n  void routes(r) {\n    r.child('/', child: (context) => const HomePage());\n    r.module('/products', module: ProductsModule());\n  }\n}\n```\n\n```dart title=\"v7\"\nfinal homeModule = createModule(\n  path: '/home',\n  register: (c) {\n    c\n      ..addSingleton<HomeStore>(HomeStore.new)\n      ..route('/', child: (ctx, state) => const HomePage())\n      ..module(productsModule); // productsModule declares path: '/products'\n  },\n);\n```\n\nTwo shifts to note:\n\n- A submodule now declares its **own** `path` (`createModule(path: '/products', ...)`),\n  so the parent just lists `c.module(productsModule)`. Override at the include site with\n  `c.module(productsModule, at: '/catalog')` only when you must.\n- Reference each module by its `final` value — composition\n  [dedups by identity](./module.md#creating-a-module).\n\n## Routes, redirects and wildcards\n\n`ChildRoute` → `c.route`. The builder now receives `(context, state)` instead of just\n`context`, and `RedirectRoute`/`WildcardRoute` are replaced by **guards**:\n\n```dart title=\"v6\"\nr.child('/product/:id', child: (context) => ProductPage(id: Modular.args.params['id']));\nr.redirect('/old', to: '/new');\nr.wildcard(child: (context) => const NotFoundPage());\n```\n\n```dart title=\"v7\"\nc.route('/product/:id', child: (ctx, state) => ProductPage(id: state['id']!));\nc.route('/old', guards: [(state) => '/new'], child: (ctx, state) => const SizedBox());\n// Unknown paths: redirect via a guard on a catch‑all you control.\n```\n\n## Dependency injection\n\n`Bind` types are gone; register constructors directly on the context. See\n[Dependency injection](./dependency-injection.md).\n\n```dart title=\"v6\"\nvoid binds(i) {\n  i.addSingleton(Repository.new);\n  i.add(ApiClient.new);\n}\n// later: final repo = Modular.get<Repository>();\n```\n\n```dart title=\"v7\"\nregister: (c) {\n  c\n    ..addSingleton<Repository>(Repository.new)\n    ..add<ApiClient>(ApiClient.new);\n}\n// later: final repo = inject<Repository>(); // or constructor injection\n```\n\n## Navigation\n\nThe global `Modular.to` facade becomes extensions on `BuildContext`:\n\n```dart title=\"v6\"\nModular.to.pushNamed('/products/42', arguments: data);\nModular.to.navigate('/home');\nModular.to.pop(result);\n```\n\n```dart title=\"v7\"\ncontext.pushNamed('/products/42', arguments: data);\ncontext.navigate('/home');\ncontext.pop(result);\n```\n\nRead params/arguments from the `state` your builder receives instead of `Modular.args`.\nNote the v7 [URL model](./navigation.md#the-url-reflects-the-stack-base): `pushNamed`\nstacks modal‑like (out of the URL) and `navigate` owns the URL.\n\n## Bootstrap\n\n`ModularApp` now owns the router config and the root‑navigator options that used to be\nset through global setters:\n\n```dart title=\"v6\"\nvoid main() {\n  Modular.setInitialRoute('/home');\n  Modular.setNavigatorKey(myKey);\n  Modular.setObservers([myObserver]);\n  runApp(ModularApp(module: AppModule(), child: const AppWidget()));\n}\n\nclass AppWidget extends StatelessWidget {\n  Widget build(context) => MaterialApp.router(routerConfig: Modular.routerConfig);\n}\n```\n\n```dart title=\"v7\"\nvoid main() {\n  runApp(\n    ModularApp(\n      module: appModule,\n      initialRoute: '/home',\n      navigatorKey: myKey,\n      navigatorObservers: [myObserver],\n      child: const AppRoot(),\n    ),\n  );\n}\n\nclass AppRoot extends StatelessWidget {\n  Widget build(context) =>\n      MaterialApp.router(routerConfig: ModularApp.routerConfigOf(context));\n}\n```\n\n## State management\n\nIf you used a separate state package wired through `Modular.get`, consider moving\npage‑local state into the route's [`provide`](./state-management.md) so it is built and\ndisposed with the page, and keeping app‑global state (theme/session) on\n`ModularApp.provide`. The durable source of truth stays a root‑owned singleton in DI.",
  ),
  DocPage(
    path: "flutter_modular/module.md",
    title: "Modules & composition",
    markdown: "# Modules & composition\n\nA **Module** is the unit of structure in Modular: it declares a scope's **dependency\ninjection** and **routes**, and nothing else. The two things that couple a Flutter app\nare exactly DI + Routes, so a module is the app's coupling map made explicit.\n\n## Creating a module\n\nBuild a module functionally with `createModule`, and store it in a top‑level `final`:\n\n```dart\nimport 'package:flutter_modular/flutter_modular.dart';\n\nfinal homeModule = createModule(\n  path: '/home',\n  register: (c) {\n    c.route('/', child: (ctx, state) => const HomePage());\n  },\n);\n```\n\nThe `register` callback receives a `ModularContext` (`c`), the single surface a module\ndeclares itself through:\n\n| On `ModularContext` | Purpose |\n|---|---|\n| `c.route(path, child:, …)` | Declare a route (see [Navigation](./navigation.md)) |\n| `c.module(other, {at})` | Include another module |\n| `c.add<T>` / `addSingleton<T>` / `addLazySingleton<T>` / `addInstance<T>` | Register a dependency (see [DI](./dependency-injection.md)) |\n\n:::warning Modules are deduplicated by identity\nComposition dedups modules by **object identity**, so always reference the same `final`\nvalue. Calling `createModule(...)` twice for the same logical module creates two\ndistinct objects and defeats the dedup.\n:::\n\n## A module's `path` decides what it is\n\nThis is the central idea of v7. Whether a module declares a `path` changes its meaning:\n\n### A module **with** a `path` is a *feature*\n\nIts routes are **flattened under that path**, and its dependencies are\n**feature‑scoped** — bound when its first route enters the stack and disposed when its\nlast route leaves (covered in [DI lifecycle](./dependency-injection.md#bind-lifecycle)).\n\n```dart\n// Mounted at /products → these relative routes become /products and /products/:id.\nfinal productsModule = createModule(\n  path: '/products',\n  register: (c) {\n    c\n      ..route('/', child: (ctx, state) => const ProductListPage())\n      ..route('/:id', child: (ctx, state) => ProductDetailPage(id: state['id']!));\n  },\n);\n```\n\nA mount path is a **static prefix**: it must start with `/` and contain no dynamic\nsegment. Dynamic `:params` belong to the routes *inside* a module, not to where the\nmodule mounts.\n\n```dart\ncreateModule(path: '/checkout', register: (_) {});  // ✅ ok\ncreateModule(register: (_) {});                      // ✅ ok — no path (see below)\ncreateModule(path: 'checkout', register: (_) {});    // ❌ AssertionError — must start with '/'\ncreateModule(path: '/users/:id', register: (_) {});  // ❌ AssertionError — no ':param' in a mount path\n```\n\n### A module **without** a `path` is *shared DI*\n\nIts dependencies are **root‑owned**: bound eagerly and never disposed — they live for\nthe whole app. This is where your Single Source of Truth belongs.\n\n```dart\n/// CORE — the shared data layer. Everything here is an app-wide singleton in this\n/// route-less module, so it is root-owned and lives for the whole app.\nfinal coreModule = createModule(\n  register: (c) {\n    c\n      ..addSingleton<ProductService>(ProductService.new)\n      ..addSingleton<ProductRepository>(ProductRepository.new)\n      ..addSingleton<AppSession>(AppSession.new);\n  },\n);\n```\n\n## Composing modules\n\nUse `c.module(...)` to include one module in another. A feature lists its sub‑features;\nthe root module is the whole app's composition:\n\n```dart\n/// THE ROOT MODULE — composition only. This file is the app's coupling map: which\n/// modules exist and how they connect.\nfinal appModule = createModule(\n  register: (c) {\n    c\n      ..module(coreModule)   // shared DI (no path) → root-owned\n      ..module(homeModule);  // feature (path '/home') → mounted there\n  },\n);\n```\n\nEach module declares its **own** `path`, so a parent usually just lists them:\n\n```dart\nfinal homeModule = createModule(\n  path: '/home',\n  register: (c) {\n    c\n      ..route('/', child: (ctx, state) => const HomePage())\n      ..module(productsModule)   // → /home/products\n      ..module(settingsModule)\n      ..module(dashboardModule);\n  },\n);\n```\n\nBecause `productsModule` is included under `homeModule` (mounted at `/home`), its routes\nland at `/home/products` and `/home/products/:id`. Composition concatenates the paths,\njust like the module tree mirrors the widget tree.\n\n### Overriding the mount with `at:`\n\n`at:` is the rare override of a module's own `path` at the include site — mount the same\nmodule somewhere else:\n\n```dart\nc.module(productsModule, at: '/catalog'); // routes become /catalog, /catalog/:id\n```\n\nMost apps never need `at:`; let each module name its own mount.\n\n## Feature vs. shell\n\n`module(...)` **flattens** a sub‑module's routes under its path — there is no visible\nshell wrapping them. When you want chrome that **persists** across child routes (a\nbottom bar, a sidebar), declare a `RouterOutlet` explicitly inside a route's\n`children`. See [Nested routes & RouterOutlet](./nested-routes.md).\n\n## Next\n\n- Register and resolve dependencies → [Dependency injection](./dependency-injection.md)\n- Declare routes, params and guards → [Navigation](./navigation.md)",
  ),
  DocPage(
    path: "flutter_modular/navigation.md",
    title: "Navigation",
    markdown: "# Navigation\n\nModular's routing is built on Navigator 2.0 and matches paths like the web: static\nsegments, dynamic `:params`, query strings, nested routes and relative paths. You\nnavigate from any widget through extensions on `BuildContext`.\n\n## Declaring routes\n\nRoutes are declared on a module's context with `c.route`:\n\n```dart\nfinal productsModule = createModule(\n  path: '/products',\n  register: (c) {\n    c\n      ..route('/', child: (ctx, state) => const ProductListPage())\n      ..route('/:id', child: (ctx, state) => ProductDetailPage(id: state['id']!));\n  },\n);\n```\n\nThe full `route` signature:\n\n```dart\nc.route(\n  String path, {\n  required ModularWidgetBuilder child,        // (BuildContext, RouteState) => Widget\n  void Function(Scoped scoped)? provide,       // page-scoped state — see State management\n  void Function(ModularContext c)? children,   // nested routes — see Nested routes\n  List<ModularGuard>? guards,                  // redirects — see below\n  TransitionType transition,                   // material | fade | none\n});\n```\n\n## RouteState\n\nEvery route `child` receives a `RouteState` — an immutable snapshot of the current\nroute:\n\n```dart\nc.route('/product/:id', child: (ctx, state) {\n  final id = state['id'];                 // path param, shorthand for state.params['id']\n  final ref = state.query['ref'];         // query string ?ref=...\n  final extra = state.arguments;          // object passed via pushNamed(arguments:)\n  return ProductDetailPage(id: id!);\n});\n```\n\n| Member | What it is |\n|---|---|\n| `state.uri` | the full resolved `Uri` (path + query) |\n| `state.params` | path params from the match, e.g. `{'id': '42'}` |\n| `state['id']` | shorthand for `state.params['id']` |\n| `state.query` | query parameters (`?a=1&b=2`) |\n| `state.arguments` | the object passed at navigation time (not in the URL) |\n\nYou can also read the current route from **outside** a route builder (e.g. a shell's\nactive‑tab highlight) with `context.routeState()`. It is **reactive** by default — the\nwidget rebuilds when the app navigates, so route‑aware chrome derives from the route\ninstead of mirroring it in local state. Pass `listen: false` to read it in a callback\nwithout subscribing.\n\n## Navigating\n\nThe navigation verbs are extension methods on `BuildContext`:\n\n```dart\ncontext.pushNamed('/products/42');   // stack a page on top\ncontext.navigate('/home');           // replace the whole stack (reset history)\ncontext.replace('/login');           // replace just the top route\ncontext.pop(result);                 // pop, delivering a result to the awaiting pushNamed\n```\n\n| Method | Effect |\n|---|---|\n| `pushNamed<T>(path, {arguments})` | Pushes a page; returns a `Future<T?>` completed by the matching `pop(result)`. |\n| `navigate(path, {arguments})` | Replaces the whole stack and **resets history**. The v7 unification of 6.x's `navigate` + `pushNavigate`. |\n| `replace<T>(path, {arguments})` | Replaces the **top** route (no back to the replaced one). |\n| `pop<T>([result])` | Pops the top route, completing its `pushNamed` future with `result`. |\n| `maybePop<T>([result])` | Pops only if there is something to pop; returns whether it did. |\n| `canPop()` | Whether `pop` would do anything. |\n| `popUntil(predicate)` | Pops until `predicate(RouteState)` holds. |\n| `popAndPushNamed<T>(path, {result, arguments})` | Pops (delivering `result`), then pushes. |\n| `pushNamedAndRemoveUntil<T>(path, predicate, {arguments})` | Pushes, then removes routes beneath it until `predicate` holds. |\n\n### The URL reflects the stack *base*\n\nThis is the v7 routing model: the URL mirrors the **base** of the navigation stack, not\nits top. So `navigate(...)` — which replaces the stack base — **changes the URL**, while\n`pushNamed(...)` stacks a page **modal‑like** that stays **out of** the URL. Use\n`navigate` for \"go here and own the address\" (tabs, top‑level destinations) and\n`pushNamed` for \"stack a detail/modal on top of where I am\".\n\n:::note\nInside a [`RouterOutlet`](./nested-routes.md), these verbs target the nearest outlet, so\nthe parent shell persists. Otherwise they target the root navigator.\n:::\n\n## Relative routes\n\nA path without a leading `/` is **relative to the route the context is on**, resolved\nlike a directory:\n\n```dart\n// On /home:\ncontext.pushNamed('dashboard');    // → /home/dashboard   (bare or ./ = one level deeper)\ncontext.pushNamed('./dashboard');  // → /home/dashboard\ncontext.pushNamed('../settings');  // → /settings         (.. climbs a level)\ncontext.pushNamed('/login');       // → /login            (leading / = absolute)\n```\n\nThis improves on 6.x's raw `Uri.resolve`, which treated `/home` as a *file* and turned\n`dashboard` into `/dashboard` (dropping `home`). Modular treats the current location as\na directory, so a bare reference goes *inside* it. Query and fragment on the reference\nare preserved (`item?ref=x`).\n\n## Passing arguments and getting results\n\n`arguments` carries an arbitrary object to the target route, recovered through\n`RouteState.arguments`; the `Future` returned by `pushNamed` delivers a result back via\n`pop`:\n\n```dart\n// Caller — pass an object, await a result:\nfinal saved = await context.pushNamed<bool>(\n  '/args/editor',\n  arguments: EditorArgs(title: 'Draft'),\n);\n\n// Target route — read the object defensively:\nc.route('/args/editor', child: (ctx, state) {\n  final args = state.arguments;\n  if (args is! EditorArgs) {\n    return const Scaffold(body: Center(child: Text('Open this from the Arguments page.')));\n  }\n  return EditorPage(args: args);\n});\n\n// Inside the editor — return a result:\ncontext.pop(true);\n```\n\n:::warning arguments are not in the URL\nUnlike `:id` path params, `arguments` is **not** part of the URL. A deep link or a web\nrefresh on `/args/editor` arrives with `arguments == null`, so always read it\ndefensively. Use path params or query for anything that must survive a refresh.\n:::\n\n## Guards\n\nA **guard** is a pure function `String? Function(RouteState)`: return a **redirect path**\nto send the user elsewhere, or `null` to allow navigation. Guards run **before** the\npage is shown.\n\n```dart\nc.route(\n  '/settings/secret',\n  guards: [\n    // Reads DI at guard-eval time via inject<T>(); a redirect is an absolute path.\n    (state) => inject<AppSession>().unlocked ? null : '/home/settings',\n  ],\n  child: (ctx, state) => const SecretPage(),\n);\n```\n\nGuards are a list, evaluated in order — the first one that returns a path wins and\nredirects. Reach dependencies inside a guard with\n[`inject<T>()`](./dependency-injection.md#resolving-with-injectt). A guard redirect is an\n**absolute** destination (there is no current context to be relative to). Use a guard to\ngate authenticated areas, or to redirect unknown paths to a 404 page.\n\n## Transitions\n\nSet a route's page transition with `transition:`:\n\n```dart\nc.route('/details', transition: TransitionType.fade, child: ...);\n```\n\n`TransitionType` is `material` (the default), `fade`, or `none` (instant).\n\n## Next\n\n- Persistent shells and tabs → [Nested routes & RouterOutlet](./nested-routes.md)\n- Page‑scoped view models → [State management](./state-management.md)",
  ),
  DocPage(
    path: "flutter_modular/nested-routes.md",
    title: "Nested routes & RouterOutlet",
    markdown: "# Nested routes & RouterOutlet\n\nSome screens are **shells**: a persistent frame — a bottom bar, a sidebar, a tab bar —\nwhose body swaps as you move between sub‑destinations. Modular models this with nested\nroutes and a `RouterOutlet`.\n\n## children vs. a flattened module\n\nThere are two ways routes nest, and they differ in one thing — whether a **shell\npersists**:\n\n- `c.module(other)` **flattens** the sub‑module's routes under its path. There is no\n  visible frame around them; each route is a full page. This is the common case.\n- A route's `children:` declares sub‑routes that render **inside a `RouterOutlet`** you\n  place in that route's widget — so the parent stays mounted while the body changes.\n  Use this when you want chrome that persists.\n\n## A persistent shell\n\nDeclare the shell route with `children`, and render a `RouterOutlet` where the body\ngoes:\n\n```dart\nfinal dashboardModule = createModule(\n  path: '/dashboard',\n  register: (c) {\n    c.route(\n      '/',\n      child: (ctx, state) => const DashboardShell(), // the frame (bottom bar)\n      children: (sub) {\n        sub\n          ..route('/', child: (ctx, state) => const FeedPage())\n          ..route('/search', child: (ctx, state) => const SearchPage())\n          ..route('/profile', child: (ctx, state) => const ProfilePage())\n          ..route('/item', child: (ctx, state) => const ItemPage());\n      },\n    );\n  },\n);\n```\n\nMounted at `/dashboard`, the children resolve to `/dashboard` (Feed), `/dashboard/search`,\n`/dashboard/profile`, `/dashboard/item`. The shell renders the matching child in its\noutlet:\n\n```dart\nclass DashboardShell extends StatefulWidget {\n  const DashboardShell({super.key});\n  @override\n  State<DashboardShell> createState() => _DashboardShellState();\n}\n\nclass _DashboardShellState extends State<DashboardShell> {\n  // The bottom bar is a SIBLING of the outlet, so it drives it through a key\n  // rather than RouterOutlet.of(context).\n  final _outlet = GlobalKey<RouterOutletState>();\n\n  static const _tabs = [\n    '/dashboard',\n    '/dashboard/search',\n    '/dashboard/profile',\n  ];\n\n  @override\n  Widget build(BuildContext context) {\n    // The active tab is DERIVED from the route — never stored. routeState()\n    // rebuilds this when the URL changes, so a deep link lights the right tab.\n    final path = context.routeState().uri.path;\n    final selected = _tabs.lastIndexWhere((t) => path == t || path.startsWith('\$t/'));\n\n    return Scaffold(\n      appBar: AppBar(title: const Text('Dashboard')),\n      body: RouterOutlet(key: _outlet),\n      bottomNavigationBar: NavigationBar(\n        selectedIndex: selected < 0 ? 0 : selected,\n        // A tab tap just navigates the outlet — no local state to flip.\n        onDestinationSelected: (i) => _outlet.currentState?.navigate(_tabs[i]),\n        destinations: const [\n          NavigationDestination(icon: Icon(Icons.dynamic_feed), label: 'Feed'),\n          NavigationDestination(icon: Icon(Icons.search), label: 'Search'),\n          NavigationDestination(icon: Icon(Icons.person), label: 'Profile'),\n        ],\n      ),\n    );\n  }\n}\n```\n\n`RouterOutlet` is a **real nested `Navigator`** with its own push/pop sub‑stack. The\nshell (`DashboardShell`) builds once and stays mounted while the body swaps — its\n`State` is preserved across tab switches.\n\n## Tabs vs. stacking inside an outlet\n\nThe two navigation verbs mean different things inside an outlet:\n\n- **`navigate(path)`** *replaces* the outlet's sub‑stack — a **tab switch** (no history\n  to pop back through). That is what the bottom bar calls.\n- **`pushNamed(path)`** *stacks* a page **inside** the same outlet, so the shell\n  persists over it. Pop returns to the tab beneath.\n\nFrom a widget **inside** the outlet you can use the `context` verbs directly — they\ntarget the nearest outlet automatically:\n\n```dart\n// Inside a tab page — stacks /dashboard/item over the current tab, shell intact:\ncontext.pushNamed('/dashboard/item');\n```\n\nThe shell above drives the outlet through a `GlobalKey<RouterOutletState>` instead,\nbecause the bottom bar is a **sibling** of the outlet (not a descendant), so there is no\nenclosing outlet on its `context`.\n\n## Back button on a shell's index page\n\nA mounted module's index page is the sole entry of its outlet's nested `Navigator`, so\n`AppBar`'s `automaticallyImplyLeading` cannot see the root stack underneath and shows no\narrow on its own. Use `context.canPop()` to decide whether to show one:\n\n```dart\nAppBar(\n  leading: context.canPop()\n      ? BackButton(onPressed: () => context.pop())\n      : null,\n);\n```\n\n## The URL follows the outlet\n\nSwitching tabs changes the URL (`/dashboard` → `/dashboard/search`) because the outlet\nreports its **base** sub‑route to the root delegate. A `pushNamed` *inside* the outlet\nleaves that base unchanged, so — consistent with the [stack‑base URL\nmodel](./navigation.md#the-url-reflects-the-stack-base) — a stacked page stays out of the\nURL while a tab switch shows up in it.\n\n:::tip One outlet, one job\nReach for `RouterOutlet` only when you want a frame that **persists** across child\nroutes. If each destination is a full page with no shared chrome, plain `module(...)`\nflattening is simpler.\n:::",
  ),
  DocPage(
    path: "flutter_modular/start.md",
    title: "Getting started",
    markdown: "# Getting started\n\nThis page builds the smallest possible Modular app — a counter — and explains each\npiece. By the end you will have a module, an app bootstrap, and a page‑scoped view\nmodel wired to a route.\n\n## Install\n\nAdd **flutter_modular** to your project:\n\n```bash\nflutter pub add flutter_modular\n```\n\nThat yields a dependency on the v7 line:\n\n```yaml title=\"pubspec.yaml\"\ndependencies:\n  flutter_modular: ^7.0.0\n```\n\n## A module is DI + Routes\n\nEverything starts with a **Module**: the object that declares a scope's dependency\ninjection and its routes. Build one functionally with `createModule`:\n\n```dart\nimport 'package:flutter_modular/flutter_modular.dart';\n\nfinal appModule = createModule(\n  register: (c) {\n    c.route(\n      '/',\n      provide: (s) => s.addChangeNotifier<CounterViewModel>(CounterViewModel.new),\n      child: (context, state) => const CounterPage(),\n    );\n  },\n);\n```\n\n- `c.route('/', child: ...)` declares the route shown at `/`. The builder receives the\n  `BuildContext` and a [`RouteState`](./navigation.md#routestate) (path params, query,\n  arguments).\n- `provide:` declares **page‑scoped state** for that route — here a `CounterViewModel`\n  built when the page mounts and `dispose()`d when it leaves. More on that in\n  [State management](./state-management.md).\n\n:::tip Store modules in a `final`\nModules are deduplicated **by identity**. Keep each one in a top‑level `final` and\nreference that same value everywhere it is composed — never `createModule(...)` twice\nfor the same logical module.\n:::\n\n## Bootstrap with ModularApp\n\n`ModularApp` is the **first widget** of your app, sitting *above* `MaterialApp`. It\nbootstraps the module once (collecting its routes + DI), owns the resulting injector,\nand builds the router config:\n\n```dart title=\"lib/main.dart\"\nimport 'package:flutter/material.dart';\nimport 'package:flutter_modular/flutter_modular.dart';\n\nvoid main() {\n  runApp(\n    ModularApp(\n      module: appModule,\n      child: const AppRoot(),\n    ),\n  );\n}\n```\n\nThe `child` is your `MaterialApp.router`. It reads the router config that `ModularApp`\nbuilt with `ModularApp.routerConfigOf(context)`:\n\n```dart\nclass AppRoot extends StatelessWidget {\n  const AppRoot({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return MaterialApp.router(\n      title: 'My Smart App',\n      routerConfig: ModularApp.routerConfigOf(context),\n    );\n  }\n}\n```\n\n:::warning\n`ModularApp` must be **above** the `MaterialApp.router` that reads\n`routerConfigOf(context)`. That position is also what makes *app‑scoped* state\n(theme, locale, session) possible — see [State management](./state-management.md#app-scoped-state).\n:::\n\n## The full counter\n\nPutting it together — a complete, runnable app (this is the package's own\n[`example`](https://github.com/Flutterando/modular/tree/master/example) in miniature):\n\n```dart title=\"lib/main.dart\"\nimport 'package:flutter/material.dart';\nimport 'package:flutter_modular/flutter_modular.dart';\n\n/// A page-scoped view model (built per page mount, disposed on exit).\nclass CounterViewModel extends ChangeNotifier {\n  int count = 0;\n\n  void increment() {\n    count++;\n    notifyListeners();\n  }\n}\n\nfinal appModule = createModule(\n  register: (c) {\n    c.route(\n      '/',\n      provide: (s) => s.addChangeNotifier<CounterViewModel>(CounterViewModel.new),\n      child: (context, state) => const CounterPage(),\n    );\n  },\n);\n\nvoid main() {\n  runApp(ModularApp(module: appModule, child: const AppRoot()));\n}\n\nclass AppRoot extends StatelessWidget {\n  const AppRoot({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return MaterialApp.router(\n      title: 'flutter_modular counter',\n      routerConfig: ModularApp.routerConfigOf(context),\n    );\n  }\n}\n\nclass CounterPage extends StatelessWidget {\n  const CounterPage({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    final vm = context.watch<CounterViewModel>(); // reactive, page-scoped\n    return Scaffold(\n      appBar: AppBar(title: const Text('Counter')),\n      body: Center(child: Text('count: \${vm.count}')),\n      floatingActionButton: FloatingActionButton(\n        onPressed: context.read<CounterViewModel>().increment,\n        child: const Icon(Icons.add),\n      ),\n    );\n  }\n}\n```\n\n## Configuring the root\n\n`ModularApp` accepts a few options for the root navigator and the entry route:\n\n```dart\nModularApp(\n  module: appModule,\n  initialRoute: '/home',            // first route when the platform reports no deep link\n  navigatorKey: myNavigatorKey,     // imperative access from outside the tree\n  navigatorObservers: [myObserver], // analytics, RouteObserver, …\n  child: const AppRoot(),\n);\n```\n\n- **`initialRoute`** is shown when the platform hands you the bare `/` (app cold‑start\n  with no deep link). A real entry URL — a web refresh on `/products/3`, an app link —\n  overrides it.\n- **`navigatorKey`** lets you reach the root `Navigator` imperatively (e.g. to show a\n  global dialog). A fresh key is created if you omit it.\n- **`navigatorObservers`** are attached to the root navigator.\n\n:::tip Clean URLs on the web\nCall `usePathUrlStrategy()` (from `package:flutter_web_plugins/url_strategy.dart`) at\nthe top of `main()` to get `/products/3` instead of `/#/products/3`. It is a no‑op off\nthe web.\n:::\n\nThat is enough to run a Modular app. Next, learn how to grow it into multiple features\nwith [Modules](./module.md), or jump to [Navigation](./navigation.md).",
  ),
  DocPage(
    path: "flutter_modular/state-management.md",
    title: "State management",
    markdown: "# State management\n\nThis is the architecture Modular pushes. State is **scoped** and has a **deterministic\nlifecycle**: a view model is built when its page mounts and disposed when the page\nleaves the stack. You don't own globals and you don't write `dispose` calls — the\nframework does. The durable truth stays in a repository/service in\n[DI](./dependency-injection.md); view models are disposable projections over it.\n\n## Page‑scoped state with `provide`\n\nDeclare a route's state in its `provide` callback. Each registration becomes a factory\nbuilt in a **page‑local injector** at mount (its own dependencies resolved from the\nmodule injector), provided to the subtree, and disposed at unmount.\n\n```dart\nfinal productsModule = createModule(\n  path: '/products',\n  register: (c) {\n    c\n      ..route(\n        '/',\n        provide: (s) => s.addChangeNotifier<ProductListViewModel>(ProductListViewModel.new),\n        child: (ctx, state) => const ProductListPage(),\n      )\n      ..route(\n        '/:id',\n        provide: (s) {\n          s\n            ..add<RealtimeConnection>(RealtimeConnection.new)\n            ..addChangeNotifier<ProductDetailViewModel>(ProductDetailViewModel.new)\n            ..addStream<int>(_viewersStream);\n        },\n        child: (ctx, state) => ProductDetailPage(id: state['id']!),\n      );\n  },\n);\n```\n\nThe rule is **`addChangeNotifier`** (a reactive view model) and **`addStream`**\n(stream‑backed state); **`add`** registers a plain non‑reactive object. The `Scoped`\nregistrar (`s`) offers:\n\n| Method | For | Reactive? | Disposed on unmount? |\n|---|---|---|---|\n| `addChangeNotifier<T>(ctor)` | a `ChangeNotifier` view model | ✅ via `watch`/`read` | ✅ `dispose()` |\n| `addStream<T>(create)` | stream‑backed state | ✅ as `StreamValue<T>` | ✅ cancels the subscription |\n| `add<T>(ctor)` | a non‑reactive object (socket, use‑case, config) | ❌ | ✅ if it implements `Disposable` |\n\nReactivity and lifecycle are **independent**: a thing can have either, both, or neither.\nFor reactive objects that don't fit the two rules above — a **BLoC**, a **Cubit**, a\ncontroller exposing a `Listenable` — there are two escape hatches,\n[`addStreamable` and `addListenable`](#exceptions-addstreamable-and-addlistenable).\n\n### addChangeNotifier — a reactive view model\n\nThe bound type is a `ChangeNotifier` (not a bare `Listenable`) precisely so disposal is\nguaranteed. A page‑scoped VM reads the source of truth instead of holding it:\n\n```dart\n/// Page-scoped: 1:1 with the list view. Reads the repository (SSoT), doesn't own truth.\nclass ProductListViewModel extends ChangeNotifier {\n  ProductListViewModel(this._repo);          // repo injected from the module graph\n  final ProductRepository _repo;\n\n  bool loading = true;\n  List<Product> products = const [];\n\n  Future<void> load() async {\n    products = await _repo.getProducts();\n    loading = false;\n    notifyListeners();\n  }\n}\n```\n\n### add — a non‑reactive resource\n\nFor something that needs lifecycle but no reactivity — a connection, a subscription\nmanager, a use‑case holding a handle. It is built as a per‑page singleton, so a view\nmodel can **inject the same instance**. If it implements `Disposable`, it is\n`dispose()`d on exit — `add` **always** checks for `Disposable`:\n\n```dart\nclass RealtimeConnection implements Disposable {\n  bool isOpen = true;\n\n  @override\n  void dispose() {\n    isOpen = false; // closed when the detail page leaves the stack\n  }\n}\n```\n\n```dart\nclass ProductDetailViewModel extends ChangeNotifier {\n  // The page's RealtimeConnection (same instance) injected alongside the repo.\n  ProductDetailViewModel(this._repo, this._connection);\n  final ProductRepository _repo;\n  final RealtimeConnection _connection;\n  bool get connected => _connection.isOpen;\n}\n```\n\n### addStream — stream‑backed state\n\n`addStream` exposes the latest value of a stream as a `StreamValue<T>`:\n\n```dart\nStream<int> _viewersStream() =>\n    Stream<int>.periodic(const Duration(seconds: 2), (i) => 40 + i);\n\n// ...provide: (s) => s.addStream<int>(_viewersStream)...\n\n// In the page:\nfinal viewers = context.watch<StreamValue<int>>().value; // latest int, or null\n```\n\n## Exceptions: addStreamable and addListenable\n\n`addChangeNotifier` and `addStream` cover the common cases. When an object's reactivity\nlives on a **property** — its `stream`, or a `Listenable` it exposes — and you want to\nexpose the **object itself** (to read its synchronous state and call its methods), reach\nfor these two escape hatches. Each takes a factory, a selector for the reactive source,\nand a (required) dispose callback:\n\n- `addStreamable<T>(ctor, (t) => t.stream, (t) => t.close())` — reactivity is a `Stream`.\n  `context.watch<T>()` returns the object; rebuilds fire on each emission.\n- `addListenable<T>(ctor, (t) => t.someListenable, (t) => t.dispose())` — reactivity is a\n  `Listenable` property.\n\n```dart\n// A controller that is NOT a ChangeNotifier but exposes one:\nclass SearchController {\n  final ValueNotifier<String> query = ValueNotifier('');\n  void dispose() => query.dispose();\n}\n\nprovide: (s) => s.addListenable<SearchController>(\n  SearchController.new,\n  (c) => c.query,        // the rebuild trigger\n  (c) => c.dispose(),    // cleanup on unmount\n);\n```\n\n:::note\nPrefer `addChangeNotifier`/`addStream`. Use `addStreamable`/`addListenable` only when the\nreactive source is a property of the object you want to expose.\n:::\n\n## BLoC and Cubit\n\nA **BLoC** or **Cubit** is exactly the streamable case: it exposes a synchronous `state`,\na `stream` of changes, and an async `close()`. Register it with `addStreamable` —\n`context.watch<T>()` returns the **BLoC/Cubit** itself, so you read `state` directly and\nrebuilds are driven by its stream:\n\n```dart\n// CounterCubit is a Cubit from the `bloc` package.\nroute(\n  '/counter',\n  provide: (s) => s.addStreamable<CounterCubit>(\n    CounterCubit.new,\n    (c) => c.stream,\n    (c) => c.close(),\n  ),\n  child: (ctx, state) {\n    final counter = ctx.watch<CounterCubit>(); // the Cubit itself\n    return Text('\${counter.state}');           // read its synchronous state\n  },\n);\n```\n\nflutter_modular has **no dependency on the `bloc` package** — `addStreamable` takes the\n`stream` and `close` as callbacks. To make this a one‑liner, add a small extension on\n`Scoped` in your app. Because both **BLoC** and **Cubit** extend `BlocBase` (which has\n`.stream` and `.close()`), a single `addBloc` covers both:\n\n```dart\nimport 'package:bloc/bloc.dart';\nimport 'package:flutter_modular/flutter_modular.dart';\n\n/// Registers a page-scoped BLoC or Cubit: reactive via its stream, closed on unmount.\nextension BlocScoped on Scoped {\n  void addBloc<B extends BlocBase<Object?>>(B Function() create) =>\n      addStreamable<B>(create, (b) => b.stream, (b) => b.close());\n}\n```\n\n```dart\n// Now registering any BLoC or Cubit is one line:\nprovide: (s) => s.addBloc<CounterCubit>(CounterCubit.new),\n```\n\n:::tip\nWith the extension above, `addBloc<MyBloc>(MyBloc.new)` works for both **BLoC** and\n**Cubit** — one line to get a page‑scoped, auto‑closed, reactive instance.\n:::\n\n## Reading state: `watch` and `read`\n\nFrom any descendant of the page, reach a provided `Listenable`:\n\n```dart\nfinal vm = context.watch<ProductListViewModel>(); // rebuilds this widget on notify\ncontext.read<ProductListViewModel>().load();       // reads without subscribing (callbacks)\n```\n\n- `context.watch<T>()` subscribes — the widget rebuilds when `T` notifies.\n- `context.read<T>()` does **not** subscribe — use it in callbacks (`onPressed`) and\n  one‑shot calls.\n\n## Granular rebuilds: `Consumer` and `Selector`\n\n`watch` rebuilds the whole widget that called it. To scope a rebuild to a sub‑tree, use\n`Consumer`; to rebuild only when a *derived value* changes, use `Selector`:\n\n```dart\n// Rebuilds only this builder when the VM notifies:\nConsumer<CartViewModel>(\n  builder: (context, cart, child) => Text('\${cart.items.length} items'),\n);\n\n// Rebuilds only when the selected value changes:\nSelector<CartViewModel, int>(\n  selector: (context, cart) => cart.items.length,\n  builder: (context, count, child) => Badge(label: Text('\$count')),\n);\n```\n\n## App‑scoped state {#app-scoped-state}\n\nPage‑scoped state lives *below* the `Navigator`, so it cannot rebuild the `MaterialApp`\nitself. For app‑global state that drives **theme, locale, or session**, declare it on\n`ModularApp.provide` — the very same `Scoped` mechanism, only anchored **above** the\n`MaterialApp`:\n\n```dart\nvoid main() {\n  runApp(\n    ModularApp(\n      module: appModule,\n      provide: (s) => s.addChangeNotifier<ThemeController>(ThemeController.new),\n      child: const AppRoot(),\n    ),\n  );\n}\n\nclass AppRoot extends StatelessWidget {\n  const AppRoot({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    final theme = context.watch<ThemeController>(); // above the MaterialApp\n    return MaterialApp.router(\n      themeMode: theme.mode,\n      theme: ThemeData.light(useMaterial3: true),\n      darkTheme: ThemeData.dark(useMaterial3: true),\n      routerConfig: ModularApp.routerConfigOf(context),\n    );\n  }\n}\n```\n\n```dart\nclass ThemeController extends ChangeNotifier {\n  ThemeMode mode = ThemeMode.light;\n  void toggle() {\n    mode = mode == ThemeMode.light ? ThemeMode.dark : ThemeMode.light;\n    notifyListeners();\n  }\n}\n```\n\nBecause it is anchored above the `Navigator`, a page deep in the tree can still reach it\nwith `context.read<ThemeController>().toggle()` — and the whole app re‑themes.\n\n## Disposable {#disposable}\n\n`Disposable` is the interface that opts a non‑reactive class into page‑scoped lifecycle:\n\n```dart\nabstract interface class Disposable {\n  void dispose();\n}\n```\n\nImplement it and register with `add` (page‑scoped) — Modular builds it in the\npage‑local injector and calls `dispose()` on unmount. Feature‑module binds that\nimplement `Disposable` (or `ChangeNotifier`) are likewise disposed when the feature\nleaves the stack; see [DI lifecycle](./dependency-injection.md#bind-lifecycle).\n\n## Why this is the architecture\n\n- **One home for the truth.** Repositories/services are root‑owned singletons; a view\n  model reads them and never becomes a competing source of truth.\n- **No floating state.** A VM exists exactly as long as its page; leaving disposes it.\n- **State management gets lighter.** Lifecycle and provision are the framework's job, so\n  whatever reactivity you choose has less to carry.",
  ),
  DocPage(
    path: "flutter_modular/testing.md",
    title: "Testing",
    markdown: "# Testing\n\nBecause a module is just data — routes + DI — you can bootstrap one in a test and assert\non it directly, with no widgets. For UI you mount the router config in a `MaterialApp`\nand pump as usual. The patterns below mirror **flutter_modular**'s own test suite.\n\n## Bootstrapping a module\n\n`bootstrapModule(module)` walks the module tree and returns a `ModularBootstrap` with\nthe matched `routes`, the `injector`, and the lifecycle `manager`:\n\n```dart\nimport 'package:flutter_modular/flutter_modular.dart';\nimport 'package:flutter_test/flutter_test.dart';\n\nclass Counter {\n  int value = 0;\n}\n\nfinal adminModule = createModule(\n  path: '/admin',\n  register: (c) => c.route('/', child: (ctx, state) => const Text('admin home')),\n);\n\nfinal appModule = createModule(\n  register: (c) {\n    c\n      ..addSingleton<Counter>(Counter.new)\n      ..route('/', child: (ctx, state) => const Text('home'))\n      ..route('/product/:id', child: (ctx, state) => Text('product \${state['id']}'))\n      ..module(adminModule);\n  },\n);\n\nvoid main() {\n  test('routes (params + mounted submodule) and binds', () {\n    final boot = bootstrapModule(appModule);\n\n    // Match a Uri to a chain of routes (root → leaf) with captured params.\n    expect(boot.routes.match(Uri.parse('/'))?.last.route.path, '/');\n    expect(boot.routes.match(Uri.parse('/product/42'))?.last.params['id'], '42');\n\n    // A mounted module is a namespace: '/admin' is a single, flattened level.\n    final admin = boot.routes.match(Uri.parse('/admin'));\n    expect(admin?.length, 1);\n    expect(admin?.last.route.path, '/admin');\n\n    // Binds resolve from the graph.\n    expect(boot.injector.get<Counter>(), isA<Counter>());\n  });\n}\n```\n\n### Asserting mount‑path rules\n\nA module's `path` must be a static prefix — a good thing to lock down in a test:\n\n```dart\nexpect(() => createModule(path: '/checkout', register: (_) {}), returnsNormally);\nexpect(() => createModule(register: (_) {}), returnsNormally);            // no path ok\nexpect(() => createModule(path: 'checkout', register: (_) {}),\n    throwsA(isA<AssertionError>()));                                       // must start with '/'\nexpect(() => createModule(path: '/users/:id', register: (_) {}),\n    throwsA(isA<AssertionError>()));                                       // no ':param'\n```\n\n## Resolving dependencies\n\nAfter `bootstrapModule`, `inject<T>()` resolves from the active graph — handy for testing\nservices and the values guards see:\n\n```dart\ntest('inject<T>() resolves from the active graph after bootstrap', () {\n  final module = createModule(register: (c) => c.addSingleton<Service>(Service.new));\n  bootstrapModule(module);\n\n  expect(inject<Service>(), isA<Service>());\n  expect(inject<Service>().hello(), 'hi');\n});\n```\n\n## Testing routes in a widget\n\nMount the bootstrapped `routes` through `modularRouterConfig` inside a\n`MaterialApp.router`, then pump. Pass `injector`/`manager` when the routes resolve binds\nor provide page‑scoped state:\n\n```dart\ntestWidgets('renders a route declared by a mounted module', (tester) async {\n  final boot = bootstrapModule(appModule);\n\n  await tester.pumpWidget(\n    MaterialApp.router(\n      routerConfig: modularRouterConfig(boot.routes, initialRoute: '/admin'),\n    ),\n  );\n  await tester.pumpAndSettle();\n\n  expect(find.text('admin home'), findsOneWidget);\n});\n```\n\n## Testing guards\n\nA [guard](./navigation.md#guards) redirects before its page is shown — assert the\ndestination renders instead:\n\n```dart\ntestWidgets('a guard redirects before the page is shown', (tester) async {\n  final boot = bootstrapModule(guardedModule);\n  await tester.pumpWidget(\n    MaterialApp.router(\n      routerConfig: modularRouterConfig(boot.routes, initialRoute: '/admin'),\n    ),\n  );\n  await tester.pumpAndSettle();\n\n  expect(find.text('login'), findsOneWidget); // guard sent us here\n  expect(find.text('admin'), findsNothing);\n});\n```\n\n## Testing page‑scoped state\n\nA page‑scoped view model is built with its module dependencies at mount and disposed on\nunmount. You can assert both — `watch` rebuilds on `notifyListeners`, and unmounting the\ntree triggers `dispose()`:\n\n```dart\ntestWidgets('builds the VM with deps, rebuilds on notify, disposes on unmount',\n    (tester) async {\n  final boot = bootstrapModule(pModule);\n\n  await tester.pumpWidget(\n    MaterialApp.router(\n      routerConfig: modularRouterConfig(\n        boot.routes,\n        injector: boot.injector,\n        initialRoute: '/product',\n      ),\n    ),\n  );\n  await tester.pumpAndSettle();\n\n  await tester.tap(find.text('load'));     // calls vm.load() via context.read\n  await tester.pumpAndSettle();\n  expect(find.text('Widget X'), findsOneWidget); // dep resolved + watch rebuilt\n\n  await tester.pumpWidget(const SizedBox()); // unmount → VM.dispose() runs\n  await tester.pumpAndSettle();\n});\n```\n\n:::tip\nSee the [`test/`](https://github.com/Flutterando/modular/tree/master/test) directory in\nthe repository for the full suite — routing, guards, `provide`/dispose, outlets,\nrelative routes, arguments, transitions and web URLs.\n:::",
  ),
  DocPage(
    path: "intro.md",
    title: "Welcome to Modular",
    markdown: "# Welcome to Modular\n\n**flutter_modular** gives a Flutter app a *structure*: it turns the two things that\ncouple an application — **Dependency Injection** and **Routes** — into small,\nself‑contained **Modules**, and ties application **state to the route lifecycle** so\nthat ownership and disposal stop being your problem.\n\n:::tip flutter_modular 7 is a ground‑up rewrite\nThe API on this page (and the whole **flutter_modular** section) describes **v7**. If\nyou are still on v6 or v5, see **(Legacy) Modular 6** / **(Legacy) Modular 5** in the\nsidebar, and the [migration guide](./flutter_modular/migration.md) when you are ready\nto upgrade.\n:::\n\n## What is Modular?\n\nIn a monolithic architecture the whole app is a single module. That is quick and\nelegant for small apps, but a larger app built that way accrues technical debt in both\nmaintenance and scalability. By dividing the app **by scope** instead, we gain:\n\n- Improved understanding of features.\n- Fewer breaking changes.\n- New, non‑conflicting features added in isolation.\n- Fewer blind spots in the main business rules.\n- Easier developer turnover.\n\nCompare a typical MVC, where every layer is global:\n\n### A typical MVC\n\n    .\n    ├── models                                  # All models\n    │   ├── auth_model.dart\n    │   ├── home_model.dart\n    │   └── product_model.dart\n    ├── controller                              # All controllers\n    │   ├── auth_controller.dart\n    │   ├── home_controller.dart\n    │   └── product_controller.dart\n    ├── views                                   # All views\n    │   ├── auth_page.dart\n    │   ├── home_page.dart\n    │   └── product_page.dart\n    ├── core                                    # Tools and utilities\n    ├── app_widget.dart                         # Main Widget containing MaterialApp\n    └── main.dart                               # runApp\n\n…with the same app divided by scope, where each feature owns its own MVC:\n\n### Structure divided by scope\n\n    .\n    ├── features                                 # All features / Modules\n    │   ├─ auth                                  # Auth's MVC\n    │   │  ├── auth_model.dart\n    │   │  ├── auth_controller.dart\n    │   │  └── auth_page.dart\n    │   ├─ home                                  # Home's MVC\n    │   │  ├── home_model.dart\n    │   │  ├── home_controller.dart\n    │   │  └── home_page.dart\n    │   └─ product                               # Product's MVC\n    │      ├── product_model.dart\n    │      ├── product_controller.dart\n    │      └── product_page.dart\n    ├── core                                     # Tools and utilities\n    ├── app_widget.dart                          # Main Widget containing MaterialApp\n    └── main.dart                                # runApp\n\nWe call this **Smart Structure**: each feature keeps its own MVC, which alone solves\nmany scalability and maintainability problems. But two things stay *global* and fight\nthe structure — **routes** and **dependency injection**. Modular exists to scope\nexactly those two. We group a feature's routes and injections into an object and call\nit a **Module**.\n\n## The architecture Modular pushes\n\nModular is opinionated on purpose. A few ideas run through the whole package:\n\n- **A Module is DI + Routes.** Those are the two coupling factors in a Flutter app, and\n  a [Module](./flutter_modular/module.md) makes exactly those visible. A module mounted\n  at a `path` is a *feature*; a module without a path is *shared DI*.\n\n- **State is page‑scoped, with a deterministic lifecycle.** A view model declared with\n  [`provide`](./flutter_modular/state-management.md) is built when its page mounts and\n  `dispose()`d when the page leaves the stack. Lifecycle becomes the framework's job —\n  which is what lets Modular take weight off whatever \"state management\" you reach for.\n\n- **The Single Source of Truth lives in a repository/service.** Durable truth is a\n  root‑owned singleton registered in [DI](./flutter_modular/dependency-injection.md);\n  view models are disposable, per‑page projections over it. They never *hold* the truth.\n\n- **Routing reads like the web.** The URL mirrors the navigation **stack base**;\n  `pushNamed` stacks a page that stays out of the URL (modal‑like); routes can be\n  **relative** to where you are, resolved like directories. See\n  [Navigation](./flutter_modular/navigation.md).\n\n## Ready to get started?\n\nModular does something powerful — componentizing routes and dependency injection — and\ndoes it simply. Head to [Getting started](./flutter_modular/start.md) and begin your\njourney toward a smart structure.\n\n## Common questions\n\n- **Does Modular work with any state‑management approach?**\n  Yes. The DI system is agnostic to the class it builds, including any reactivity. On\n  top of that, Modular ships its own page‑scoped state (`ChangeNotifier`, streams,\n  `Consumer`/`Selector`) so for many apps you need nothing else.\n\n- **Can I use dynamic routes?**\n  Yes — the route tree matches like the web: dynamic `:params`, query strings, relative\n  paths, nested routes and persistent shells (`RouterOutlet`) are all supported. Use a\n  route [guard](./flutter_modular/navigation.md#guards) to redirect (e.g. to a 404 or a\n  login page).\n\n- **Do I need a Module for every feature?**\n  No. Create a module when a feature earns its own boundary — its own routes and\n  injections — or keep small things in a parent module.\n\n- **I'm writing pure Dart, not Flutter.**\n  Use [`auto_injector`](https://pub.dev/packages/auto_injector) directly — it is the DI\n  engine **flutter_modular** is built on.",
  ),
];

const List<DocChunk> docChunks = [
  DocChunk(
    pagePath: "flutter_modular/dependency-injection.md",
    pageTitle: "Dependency injection",
    heading: "",
    anchor: "",
    text: "# Dependency injection\n\nModular's DI is powered by [`auto_injector`](https://pub.dev/packages/auto_injector).\nYou register constructors on a module's `ModularContext`; Modular resolves each\ndependency's own constructor arguments from the graph automatically.",
  ),
  DocChunk(
    pagePath: "flutter_modular/dependency-injection.md",
    pageTitle: "Dependency injection",
    heading: "Registering dependencies",
    anchor: "registering-dependencies",
    text: "## Registering dependencies\n\nInside `register`, declare dependencies on the context (`c`):\n\n```dart\nfinal coreModule = createModule(\n  register: (c) {\n    c\n      ..addSingleton<ProductService>(ProductService.new)\n      ..addSingleton<ProductRepository>(ProductRepository.new) // gets ProductService injected\n      ..addSingleton<AppSession>(AppSession.new);\n  },\n);\n```\n\n| Method | Builds | Lifetime |\n|---|---|---|\n| `add<T>(T Function(...))` | a **new** instance every time it is resolved | per resolution |\n| `addSingleton<T>(T Function(...))` | once, **eagerly** at registration | one shared instance |\n| `addLazySingleton<T>(T Function(...))` | once, on **first** resolution | one shared instance |\n| `addInstance<T>(T value)` | nothing — registers an existing object | the value you pass |\n\nYou pass a **constructor reference** (e.g. `ProductRepository.new`), not an instance.\n`auto_injector` reads its parameters and supplies them from the graph, so a repository\nthat takes a service in its constructor just works once both are registered.",
  ),
  DocChunk(
    pagePath: "flutter_modular/dependency-injection.md",
    pageTitle: "Dependency injection",
    heading: "Resolving with `inject<T>()`",
    anchor: "resolving-with-inject-t",
    text: "## Resolving with `inject<T>()`\n\nConstructor injection covers most needs — a class declares what it needs as constructor\nparameters and the graph fills them in. When you need to reach a dependency from a place\nthat has no constructor — a route **guard**, a callback, a top‑level function — use\n`inject<T>()`:\n\n```dart\nc.route(\n  '/settings/secret',\n  // inject<T>() reads DI at guard-eval time without exposing the injector object.\n  guards: [(state) => inject<AppSession>().unlocked ? null : '/home/settings'],\n  child: (ctx, state) => const SecretPage(),\n);\n```\n\n`inject<T>()` resolves `T` from the **live** module graph (the injector object itself\nstays private — Angular‑style). It throws a `StateError` if no Modular app has\nbootstrapped yet, and because it reads the live graph, a feature module's dependencies\nare reachable through it only while that feature is active.",
  ),
  DocChunk(
    pagePath: "flutter_modular/dependency-injection.md",
    pageTitle: "Dependency injection",
    heading: "Bind lifecycle",
    anchor: "bind-lifecycle",
    text: "## Bind lifecycle\n\n*Where* a dependency is registered decides *how long* it lives — this is the other half\nof the module `path` rule from [Modules](./module.md#a-modules-path-decides-what-it-is).",
  ),
  DocChunk(
    pagePath: "flutter_modular/dependency-injection.md",
    pageTitle: "Dependency injection",
    heading: "Root‑owned (shared DI, never disposed)",
    anchor: "root-owned-shared-di-never-disposed",
    text: "### Root‑owned (shared DI, never disposed)\n\nDependencies in a **path‑less** module are committed **eagerly** at bootstrap and live\nfor the whole app. They are your durable truth — repositories, services, sessions. A\nleaving route never disposes them.\n\n```dart\nfinal coreModule = createModule(            // no path → root-owned\n  register: (c) => c.addSingleton<ProductRepository>(ProductRepository.new),\n);\n```",
  ),
  DocChunk(
    pagePath: "flutter_modular/dependency-injection.md",
    pageTitle: "Dependency injection",
    heading: "Feature‑scoped (bound on entry, disposed on exit)",
    anchor: "feature-scoped-bound-on-entry-disposed-on-exit",
    text: "### Feature‑scoped (bound on entry, disposed on exit)\n\nDependencies in a module **with a `path`** are bound lazily when the feature's **first\nroute enters** the stack, and disposed when its **last route leaves**. Disposal is\nautomatic for `ChangeNotifier`s and [`Disposable`](./state-management.md#disposable)s —\ntheir `dispose()` is called.\n\n```dart\nfinal productsModule = createModule(\n  path: '/products',                        // feature → binds disposed when it leaves\n  register: (c) {\n    c.add<ProductSearchController>(ProductSearchController.new);\n    // ...routes...\n  },\n);\n```\n\nThis mirrors how the \"active path list\" worked in 6.x: a feature's resources exist only\nwhile the feature is on screen.\n\n:::tip Where should a dependency live?\nPut the **source of truth** (repositories, services, app session) in a root‑owned\nmodule — it must outlive any single page. Put **feature‑local** machinery in the\nfeature's own module so it is cleaned up when the user leaves. For state that is 1:1\nwith a single page, prefer page‑scoped [`provide`](./state-management.md) over a module\nbind.\n:::",
  ),
  DocChunk(
    pagePath: "flutter_modular/dependency-injection.md",
    pageTitle: "Dependency injection",
    heading: "Next",
    anchor: "next",
    text: "## Next\n\n- Bind state to a page's lifecycle → [State management](./state-management.md)\n- Use dependencies in guards → [Navigation › Guards](./navigation.md#guards)",
  ),
  DocChunk(
    pagePath: "flutter_modular/mcp-server.md",
    pageTitle: "Docs MCP server",
    heading: "",
    anchor: "",
    text: "# Docs MCP server\n\n`flutter_modular_docs_mcp` is an [MCP](https://modelcontextprotocol.io) server that\nserves the **most up‑to‑date flutter_modular documentation** to your AI coding\nassistant. With it connected, your assistant searches and reads these real docs instead\nof guessing — it exposes a `search_docs` tool (keyword search) and a `read_doc` tool\n(fetch a full page).",
  ),
  DocChunk(
    pagePath: "flutter_modular/mcp-server.md",
    pageTitle: "Docs MCP server",
    heading: "Install",
    anchor: "install",
    text: "## Install\n\nYou need the [Dart SDK](https://dart.dev/get-dart). Activate the server from pub.dev:\n\n```bash\ndart pub global activate flutter_modular_docs_mcp\n```\n\nMake sure `~/.pub-cache/bin` is on your `PATH` so the `flutter_modular_docs_mcp` command\nresolves. Re‑run the same command anytime to update to the latest docs.",
  ),
  DocChunk(
    pagePath: "flutter_modular/mcp-server.md",
    pageTitle: "Docs MCP server",
    heading: "Register it in your assistant",
    anchor: "register-it-in-your-assistant",
    text: "## Register it in your assistant\n\nEvery MCP client has its own configuration, so instead of documenting each one, **paste\nthe prompt below into your agentic IDE or harness** (Claude Code, Cursor, Windsurf, VS\nCode, Zed, …) and let the agent install and register it for you:\n\n```text\nInstall and register the flutter_modular docs MCP server for me.\n\n1. Make sure the Dart SDK is available, then run:\n     dart pub global activate flutter_modular_docs_mcp\n   Ensure ~/.pub-cache/bin is on PATH so the `flutter_modular_docs_mcp` command resolves.\n\n2. Register it as a stdio MCP server in whatever client you are running in\n   (Claude Code, Cursor, Windsurf, VS Code, Zed, ...), with:\n     name:    flutter_modular_docs\n     command: flutter_modular_docs_mcp\n     type:    stdio\n   Use this client's own mechanism. For example, for Claude Code:\n     claude mcp add -s user flutter_modular_docs -- flutter_modular_docs_mcp\n   For other clients, edit their MCP config (e.g. mcp.json / settings) accordingly.\n\n3. Start a new session if the client requires it, then verify the server connects\n   and that the `search_docs` and `read_doc` tools are available.\n```\n\nOnce connected, ask your assistant anything about flutter_modular and it will ground its\nanswers in this documentation.",
  ),
  DocChunk(
    pagePath: "flutter_modular/migration.md",
    pageTitle: "Migrating from v6 to v7",
    heading: "",
    anchor: "",
    text: "# Migrating from v6 to v7\n\n**flutter_modular 7 is a ground‑up rewrite.** The concepts you know — modules, routes,\ndependency injection — are all here, but the API changed shape: a module is now a small\nfunctional declaration, navigation moved onto `BuildContext`, and state is built into the\nroute lifecycle. This guide maps the old API to the new one.\n\n:::note\nLooking for **v5 → v6**? That guide is preserved under **(Legacy) Modular 6** in the\nsidebar.\n:::",
  ),
  DocChunk(
    pagePath: "flutter_modular/migration.md",
    pageTitle: "Migrating from v6 to v7",
    heading: "At a glance",
    anchor: "at-a-glance",
    text: "## At a glance\n\n| Area | v6 | v7 |\n|---|---|---|\n| Module | `class X extends Module { binds(i){} routes(r){} }` | `final x = createModule(register: (c) {...})` |\n| Child route | `r.child('/', child: (ctx) => P())` | `c.route('/', child: (ctx, state) => P())` |\n| Submodule | `r.module('/x', module: XModule())` | `c.module(xModule)` (module declares its own `path`) |\n| Redirect route | `RedirectRoute('/x', to: '/y')` | a [guard](./navigation.md#guards) returning `'/y'` |\n| Wildcard / 404 | `WildcardRoute(child: ...)` | a guard that redirects unknown paths |\n| Register a dep | `i.addSingleton(X.new)` in `binds` | `c.addSingleton<X>(X.new)` in `register` |\n| Resolve a dep | `Modular.get<X>()` | `inject<X>()` or constructor injection |\n| Navigate | `Modular.to.pushNamed('/x')` | `context.pushNamed('/x')` |\n| Replace stack | `Modular.to.navigate('/x')` | `context.navigate('/x')` |\n| Pop | `Modular.to.pop()` | `context.pop()` |\n| Route args | `Modular.args.data` / `args.params['id']` | `state.arguments` / `state['id']` |\n| Router config | `Modular.routerConfig` | `ModularApp.routerConfigOf(context)` |\n| Navigator key / observers / initial route | `Modular.setNavigatorKey/...` | `ModularApp(navigatorKey:, navigatorObservers:, initialRoute:)` |\n| Page state | bring‑your‑own | `route(provide:)` + `context.watch`/`read` |",
  ),
  DocChunk(
    pagePath: "flutter_modular/migration.md",
    pageTitle: "Migrating from v6 to v7",
    heading: "Modules",
    anchor: "modules",
    text: "## Modules\n\nThe `binds`/`routes` method pair becomes a single `register` callback, and the module is\na `final` value rather than a class you instantiate.\n\n```dart title=\"v6\"\nclass HomeModule extends Module {\n  @override\n  void binds(i) {\n    i.addSingleton(HomeStore.new);\n  }\n\n  @override\n  void routes(r) {\n    r.child('/', child: (context) => const HomePage());\n    r.module('/products', module: ProductsModule());\n  }\n}\n```\n\n```dart title=\"v7\"\nfinal homeModule = createModule(\n  path: '/home',\n  register: (c) {\n    c\n      ..addSingleton<HomeStore>(HomeStore.new)\n      ..route('/', child: (ctx, state) => const HomePage())\n      ..module(productsModule); // productsModule declares path: '/products'\n  },\n);\n```\n\nTwo shifts to note:\n\n- A submodule now declares its **own** `path` (`createModule(path: '/products', ...)`),\n  so the parent just lists `c.module(productsModule)`. Override at the include site with\n  `c.module(productsModule, at: '/catalog')` only when you must.\n- Reference each module by its `final` value — composition\n  [dedups by identity](./module.md#creating-a-module).",
  ),
  DocChunk(
    pagePath: "flutter_modular/migration.md",
    pageTitle: "Migrating from v6 to v7",
    heading: "Routes, redirects and wildcards",
    anchor: "routes-redirects-and-wildcards",
    text: "## Routes, redirects and wildcards\n\n`ChildRoute` → `c.route`. The builder now receives `(context, state)` instead of just\n`context`, and `RedirectRoute`/`WildcardRoute` are replaced by **guards**:\n\n```dart title=\"v6\"\nr.child('/product/:id', child: (context) => ProductPage(id: Modular.args.params['id']));\nr.redirect('/old', to: '/new');\nr.wildcard(child: (context) => const NotFoundPage());\n```\n\n```dart title=\"v7\"\nc.route('/product/:id', child: (ctx, state) => ProductPage(id: state['id']!));\nc.route('/old', guards: [(state) => '/new'], child: (ctx, state) => const SizedBox());\n// Unknown paths: redirect via a guard on a catch‑all you control.\n```",
  ),
  DocChunk(
    pagePath: "flutter_modular/migration.md",
    pageTitle: "Migrating from v6 to v7",
    heading: "Dependency injection",
    anchor: "dependency-injection",
    text: "## Dependency injection\n\n`Bind` types are gone; register constructors directly on the context. See\n[Dependency injection](./dependency-injection.md).\n\n```dart title=\"v6\"\nvoid binds(i) {\n  i.addSingleton(Repository.new);\n  i.add(ApiClient.new);\n}\n// later: final repo = Modular.get<Repository>();\n```\n\n```dart title=\"v7\"\nregister: (c) {\n  c\n    ..addSingleton<Repository>(Repository.new)\n    ..add<ApiClient>(ApiClient.new);\n}\n// later: final repo = inject<Repository>(); // or constructor injection\n```",
  ),
  DocChunk(
    pagePath: "flutter_modular/migration.md",
    pageTitle: "Migrating from v6 to v7",
    heading: "Navigation",
    anchor: "navigation",
    text: "## Navigation\n\nThe global `Modular.to` facade becomes extensions on `BuildContext`:\n\n```dart title=\"v6\"\nModular.to.pushNamed('/products/42', arguments: data);\nModular.to.navigate('/home');\nModular.to.pop(result);\n```\n\n```dart title=\"v7\"\ncontext.pushNamed('/products/42', arguments: data);\ncontext.navigate('/home');\ncontext.pop(result);\n```\n\nRead params/arguments from the `state` your builder receives instead of `Modular.args`.\nNote the v7 [URL model](./navigation.md#the-url-reflects-the-stack-base): `pushNamed`\nstacks modal‑like (out of the URL) and `navigate` owns the URL.",
  ),
  DocChunk(
    pagePath: "flutter_modular/migration.md",
    pageTitle: "Migrating from v6 to v7",
    heading: "Bootstrap",
    anchor: "bootstrap",
    text: "## Bootstrap\n\n`ModularApp` now owns the router config and the root‑navigator options that used to be\nset through global setters:\n\n```dart title=\"v6\"\nvoid main() {\n  Modular.setInitialRoute('/home');\n  Modular.setNavigatorKey(myKey);\n  Modular.setObservers([myObserver]);\n  runApp(ModularApp(module: AppModule(), child: const AppWidget()));\n}\n\nclass AppWidget extends StatelessWidget {\n  Widget build(context) => MaterialApp.router(routerConfig: Modular.routerConfig);\n}\n```\n\n```dart title=\"v7\"\nvoid main() {\n  runApp(\n    ModularApp(\n      module: appModule,\n      initialRoute: '/home',\n      navigatorKey: myKey,\n      navigatorObservers: [myObserver],\n      child: const AppRoot(),\n    ),\n  );\n}\n\nclass AppRoot extends StatelessWidget {\n  Widget build(context) =>\n      MaterialApp.router(routerConfig: ModularApp.routerConfigOf(context));\n}\n```",
  ),
  DocChunk(
    pagePath: "flutter_modular/migration.md",
    pageTitle: "Migrating from v6 to v7",
    heading: "State management",
    anchor: "state-management",
    text: "## State management\n\nIf you used a separate state package wired through `Modular.get`, consider moving\npage‑local state into the route's [`provide`](./state-management.md) so it is built and\ndisposed with the page, and keeping app‑global state (theme/session) on\n`ModularApp.provide`. The durable source of truth stays a root‑owned singleton in DI.",
  ),
  DocChunk(
    pagePath: "flutter_modular/module.md",
    pageTitle: "Modules & composition",
    heading: "",
    anchor: "",
    text: "# Modules & composition\n\nA **Module** is the unit of structure in Modular: it declares a scope's **dependency\ninjection** and **routes**, and nothing else. The two things that couple a Flutter app\nare exactly DI + Routes, so a module is the app's coupling map made explicit.",
  ),
  DocChunk(
    pagePath: "flutter_modular/module.md",
    pageTitle: "Modules & composition",
    heading: "Creating a module",
    anchor: "creating-a-module",
    text: "## Creating a module\n\nBuild a module functionally with `createModule`, and store it in a top‑level `final`:\n\n```dart\nimport 'package:flutter_modular/flutter_modular.dart';\n\nfinal homeModule = createModule(\n  path: '/home',\n  register: (c) {\n    c.route('/', child: (ctx, state) => const HomePage());\n  },\n);\n```\n\nThe `register` callback receives a `ModularContext` (`c`), the single surface a module\ndeclares itself through:\n\n| On `ModularContext` | Purpose |\n|---|---|\n| `c.route(path, child:, …)` | Declare a route (see [Navigation](./navigation.md)) |\n| `c.module(other, {at})` | Include another module |\n| `c.add<T>` / `addSingleton<T>` / `addLazySingleton<T>` / `addInstance<T>` | Register a dependency (see [DI](./dependency-injection.md)) |\n\n:::warning Modules are deduplicated by identity\nComposition dedups modules by **object identity**, so always reference the same `final`\nvalue. Calling `createModule(...)` twice for the same logical module creates two\ndistinct objects and defeats the dedup.\n:::",
  ),
  DocChunk(
    pagePath: "flutter_modular/module.md",
    pageTitle: "Modules & composition",
    heading: "A module's `path` decides what it is",
    anchor: "a-module-s-path-decides-what-it-is",
    text: "## A module's `path` decides what it is\n\nThis is the central idea of v7. Whether a module declares a `path` changes its meaning:",
  ),
  DocChunk(
    pagePath: "flutter_modular/module.md",
    pageTitle: "Modules & composition",
    heading: "A module **with** a `path` is a *feature*",
    anchor: "a-module-with-a-path-is-a-feature",
    text: "### A module **with** a `path` is a *feature*\n\nIts routes are **flattened under that path**, and its dependencies are\n**feature‑scoped** — bound when its first route enters the stack and disposed when its\nlast route leaves (covered in [DI lifecycle](./dependency-injection.md#bind-lifecycle)).\n\n```dart\n// Mounted at /products → these relative routes become /products and /products/:id.\nfinal productsModule = createModule(\n  path: '/products',\n  register: (c) {\n    c\n      ..route('/', child: (ctx, state) => const ProductListPage())\n      ..route('/:id', child: (ctx, state) => ProductDetailPage(id: state['id']!));\n  },\n);\n```\n\nA mount path is a **static prefix**: it must start with `/` and contain no dynamic\nsegment. Dynamic `:params` belong to the routes *inside* a module, not to where the\nmodule mounts.\n\n```dart\ncreateModule(path: '/checkout', register: (_) {});  // ✅ ok\ncreateModule(register: (_) {});                      // ✅ ok — no path (see below)\ncreateModule(path: 'checkout', register: (_) {});    // ❌ AssertionError — must start with '/'\ncreateModule(path: '/users/:id', register: (_) {});  // ❌ AssertionError — no ':param' in a mount path\n```",
  ),
  DocChunk(
    pagePath: "flutter_modular/module.md",
    pageTitle: "Modules & composition",
    heading: "A module **without** a `path` is *shared DI*",
    anchor: "a-module-without-a-path-is-shared-di",
    text: "### A module **without** a `path` is *shared DI*\n\nIts dependencies are **root‑owned**: bound eagerly and never disposed — they live for\nthe whole app. This is where your Single Source of Truth belongs.\n\n```dart\n/// CORE — the shared data layer. Everything here is an app-wide singleton in this\n/// route-less module, so it is root-owned and lives for the whole app.\nfinal coreModule = createModule(\n  register: (c) {\n    c\n      ..addSingleton<ProductService>(ProductService.new)\n      ..addSingleton<ProductRepository>(ProductRepository.new)\n      ..addSingleton<AppSession>(AppSession.new);\n  },\n);\n```",
  ),
  DocChunk(
    pagePath: "flutter_modular/module.md",
    pageTitle: "Modules & composition",
    heading: "Composing modules",
    anchor: "composing-modules",
    text: "## Composing modules\n\nUse `c.module(...)` to include one module in another. A feature lists its sub‑features;\nthe root module is the whole app's composition:\n\n```dart\n/// THE ROOT MODULE — composition only. This file is the app's coupling map: which\n/// modules exist and how they connect.\nfinal appModule = createModule(\n  register: (c) {\n    c\n      ..module(coreModule)   // shared DI (no path) → root-owned\n      ..module(homeModule);  // feature (path '/home') → mounted there\n  },\n);\n```\n\nEach module declares its **own** `path`, so a parent usually just lists them:\n\n```dart\nfinal homeModule = createModule(\n  path: '/home',\n  register: (c) {\n    c\n      ..route('/', child: (ctx, state) => const HomePage())\n      ..module(productsModule)   // → /home/products\n      ..module(settingsModule)\n      ..module(dashboardModule);\n  },\n);\n```\n\nBecause `productsModule` is included under `homeModule` (mounted at `/home`), its routes\nland at `/home/products` and `/home/products/:id`. Composition concatenates the paths,\njust like the module tree mirrors the widget tree.",
  ),
  DocChunk(
    pagePath: "flutter_modular/module.md",
    pageTitle: "Modules & composition",
    heading: "Overriding the mount with `at:`",
    anchor: "overriding-the-mount-with-at",
    text: "### Overriding the mount with `at:`\n\n`at:` is the rare override of a module's own `path` at the include site — mount the same\nmodule somewhere else:\n\n```dart\nc.module(productsModule, at: '/catalog'); // routes become /catalog, /catalog/:id\n```\n\nMost apps never need `at:`; let each module name its own mount.",
  ),
  DocChunk(
    pagePath: "flutter_modular/module.md",
    pageTitle: "Modules & composition",
    heading: "Feature vs. shell",
    anchor: "feature-vs-shell",
    text: "## Feature vs. shell\n\n`module(...)` **flattens** a sub‑module's routes under its path — there is no visible\nshell wrapping them. When you want chrome that **persists** across child routes (a\nbottom bar, a sidebar), declare a `RouterOutlet` explicitly inside a route's\n`children`. See [Nested routes & RouterOutlet](./nested-routes.md).",
  ),
  DocChunk(
    pagePath: "flutter_modular/module.md",
    pageTitle: "Modules & composition",
    heading: "Next",
    anchor: "next",
    text: "## Next\n\n- Register and resolve dependencies → [Dependency injection](./dependency-injection.md)\n- Declare routes, params and guards → [Navigation](./navigation.md)",
  ),
  DocChunk(
    pagePath: "flutter_modular/navigation.md",
    pageTitle: "Navigation",
    heading: "",
    anchor: "",
    text: "# Navigation\n\nModular's routing is built on Navigator 2.0 and matches paths like the web: static\nsegments, dynamic `:params`, query strings, nested routes and relative paths. You\nnavigate from any widget through extensions on `BuildContext`.",
  ),
  DocChunk(
    pagePath: "flutter_modular/navigation.md",
    pageTitle: "Navigation",
    heading: "Declaring routes",
    anchor: "declaring-routes",
    text: "## Declaring routes\n\nRoutes are declared on a module's context with `c.route`:\n\n```dart\nfinal productsModule = createModule(\n  path: '/products',\n  register: (c) {\n    c\n      ..route('/', child: (ctx, state) => const ProductListPage())\n      ..route('/:id', child: (ctx, state) => ProductDetailPage(id: state['id']!));\n  },\n);\n```\n\nThe full `route` signature:\n\n```dart\nc.route(\n  String path, {\n  required ModularWidgetBuilder child,        // (BuildContext, RouteState) => Widget\n  void Function(Scoped scoped)? provide,       // page-scoped state — see State management\n  void Function(ModularContext c)? children,   // nested routes — see Nested routes\n  List<ModularGuard>? guards,                  // redirects — see below\n  TransitionType transition,                   // material | fade | none\n});\n```",
  ),
  DocChunk(
    pagePath: "flutter_modular/navigation.md",
    pageTitle: "Navigation",
    heading: "RouteState",
    anchor: "routestate",
    text: "## RouteState\n\nEvery route `child` receives a `RouteState` — an immutable snapshot of the current\nroute:\n\n```dart\nc.route('/product/:id', child: (ctx, state) {\n  final id = state['id'];                 // path param, shorthand for state.params['id']\n  final ref = state.query['ref'];         // query string ?ref=...\n  final extra = state.arguments;          // object passed via pushNamed(arguments:)\n  return ProductDetailPage(id: id!);\n});\n```\n\n| Member | What it is |\n|---|---|\n| `state.uri` | the full resolved `Uri` (path + query) |\n| `state.params` | path params from the match, e.g. `{'id': '42'}` |\n| `state['id']` | shorthand for `state.params['id']` |\n| `state.query` | query parameters (`?a=1&b=2`) |\n| `state.arguments` | the object passed at navigation time (not in the URL) |\n\nYou can also read the current route from **outside** a route builder (e.g. a shell's\nactive‑tab highlight) with `context.routeState()`. It is **reactive** by default — the\nwidget rebuilds when the app navigates, so route‑aware chrome derives from the route\ninstead of mirroring it in local state. Pass `listen: false` to read it in a callback\nwithout subscribing.",
  ),
  DocChunk(
    pagePath: "flutter_modular/navigation.md",
    pageTitle: "Navigation",
    heading: "Navigating",
    anchor: "navigating",
    text: "## Navigating\n\nThe navigation verbs are extension methods on `BuildContext`:\n\n```dart\ncontext.pushNamed('/products/42');   // stack a page on top\ncontext.navigate('/home');           // replace the whole stack (reset history)\ncontext.replace('/login');           // replace just the top route\ncontext.pop(result);                 // pop, delivering a result to the awaiting pushNamed\n```\n\n| Method | Effect |\n|---|---|\n| `pushNamed<T>(path, {arguments})` | Pushes a page; returns a `Future<T?>` completed by the matching `pop(result)`. |\n| `navigate(path, {arguments})` | Replaces the whole stack and **resets history**. The v7 unification of 6.x's `navigate` + `pushNavigate`. |\n| `replace<T>(path, {arguments})` | Replaces the **top** route (no back to the replaced one). |\n| `pop<T>([result])` | Pops the top route, completing its `pushNamed` future with `result`. |\n| `maybePop<T>([result])` | Pops only if there is something to pop; returns whether it did. |\n| `canPop()` | Whether `pop` would do anything. |\n| `popUntil(predicate)` | Pops until `predicate(RouteState)` holds. |\n| `popAndPushNamed<T>(path, {result, arguments})` | Pops (delivering `result`), then pushes. |\n| `pushNamedAndRemoveUntil<T>(path, predicate, {arguments})` | Pushes, then removes routes beneath it until `predicate` holds. |",
  ),
  DocChunk(
    pagePath: "flutter_modular/navigation.md",
    pageTitle: "Navigation",
    heading: "The URL reflects the stack *base*",
    anchor: "the-url-reflects-the-stack-base",
    text: "### The URL reflects the stack *base*\n\nThis is the v7 routing model: the URL mirrors the **base** of the navigation stack, not\nits top. So `navigate(...)` — which replaces the stack base — **changes the URL**, while\n`pushNamed(...)` stacks a page **modal‑like** that stays **out of** the URL. Use\n`navigate` for \"go here and own the address\" (tabs, top‑level destinations) and\n`pushNamed` for \"stack a detail/modal on top of where I am\".\n\n:::note\nInside a [`RouterOutlet`](./nested-routes.md), these verbs target the nearest outlet, so\nthe parent shell persists. Otherwise they target the root navigator.\n:::",
  ),
  DocChunk(
    pagePath: "flutter_modular/navigation.md",
    pageTitle: "Navigation",
    heading: "Relative routes",
    anchor: "relative-routes",
    text: "## Relative routes\n\nA path without a leading `/` is **relative to the route the context is on**, resolved\nlike a directory:\n\n```dart\n// On /home:\ncontext.pushNamed('dashboard');    // → /home/dashboard   (bare or ./ = one level deeper)\ncontext.pushNamed('./dashboard');  // → /home/dashboard\ncontext.pushNamed('../settings');  // → /settings         (.. climbs a level)\ncontext.pushNamed('/login');       // → /login            (leading / = absolute)\n```\n\nThis improves on 6.x's raw `Uri.resolve`, which treated `/home` as a *file* and turned\n`dashboard` into `/dashboard` (dropping `home`). Modular treats the current location as\na directory, so a bare reference goes *inside* it. Query and fragment on the reference\nare preserved (`item?ref=x`).",
  ),
  DocChunk(
    pagePath: "flutter_modular/navigation.md",
    pageTitle: "Navigation",
    heading: "Passing arguments and getting results",
    anchor: "passing-arguments-and-getting-results",
    text: "## Passing arguments and getting results\n\n`arguments` carries an arbitrary object to the target route, recovered through\n`RouteState.arguments`; the `Future` returned by `pushNamed` delivers a result back via\n`pop`:\n\n```dart\n// Caller — pass an object, await a result:\nfinal saved = await context.pushNamed<bool>(\n  '/args/editor',\n  arguments: EditorArgs(title: 'Draft'),\n);\n\n// Target route — read the object defensively:\nc.route('/args/editor', child: (ctx, state) {\n  final args = state.arguments;\n  if (args is! EditorArgs) {\n    return const Scaffold(body: Center(child: Text('Open this from the Arguments page.')));\n  }\n  return EditorPage(args: args);\n});\n\n// Inside the editor — return a result:\ncontext.pop(true);\n```\n\n:::warning arguments are not in the URL\nUnlike `:id` path params, `arguments` is **not** part of the URL. A deep link or a web\nrefresh on `/args/editor` arrives with `arguments == null`, so always read it\ndefensively. Use path params or query for anything that must survive a refresh.\n:::",
  ),
  DocChunk(
    pagePath: "flutter_modular/navigation.md",
    pageTitle: "Navigation",
    heading: "Guards",
    anchor: "guards",
    text: "## Guards\n\nA **guard** is a pure function `String? Function(RouteState)`: return a **redirect path**\nto send the user elsewhere, or `null` to allow navigation. Guards run **before** the\npage is shown.\n\n```dart\nc.route(\n  '/settings/secret',\n  guards: [\n    // Reads DI at guard-eval time via inject<T>(); a redirect is an absolute path.\n    (state) => inject<AppSession>().unlocked ? null : '/home/settings',\n  ],\n  child: (ctx, state) => const SecretPage(),\n);\n```\n\nGuards are a list, evaluated in order — the first one that returns a path wins and\nredirects. Reach dependencies inside a guard with\n[`inject<T>()`](./dependency-injection.md#resolving-with-injectt). A guard redirect is an\n**absolute** destination (there is no current context to be relative to). Use a guard to\ngate authenticated areas, or to redirect unknown paths to a 404 page.",
  ),
  DocChunk(
    pagePath: "flutter_modular/navigation.md",
    pageTitle: "Navigation",
    heading: "Transitions",
    anchor: "transitions",
    text: "## Transitions\n\nSet a route's page transition with `transition:`:\n\n```dart\nc.route('/details', transition: TransitionType.fade, child: ...);\n```\n\n`TransitionType` is `material` (the default), `fade`, or `none` (instant).",
  ),
  DocChunk(
    pagePath: "flutter_modular/navigation.md",
    pageTitle: "Navigation",
    heading: "Next",
    anchor: "next",
    text: "## Next\n\n- Persistent shells and tabs → [Nested routes & RouterOutlet](./nested-routes.md)\n- Page‑scoped view models → [State management](./state-management.md)",
  ),
  DocChunk(
    pagePath: "flutter_modular/nested-routes.md",
    pageTitle: "Nested routes & RouterOutlet",
    heading: "",
    anchor: "",
    text: "# Nested routes & RouterOutlet\n\nSome screens are **shells**: a persistent frame — a bottom bar, a sidebar, a tab bar —\nwhose body swaps as you move between sub‑destinations. Modular models this with nested\nroutes and a `RouterOutlet`.",
  ),
  DocChunk(
    pagePath: "flutter_modular/nested-routes.md",
    pageTitle: "Nested routes & RouterOutlet",
    heading: "children vs. a flattened module",
    anchor: "children-vs-a-flattened-module",
    text: "## children vs. a flattened module\n\nThere are two ways routes nest, and they differ in one thing — whether a **shell\npersists**:\n\n- `c.module(other)` **flattens** the sub‑module's routes under its path. There is no\n  visible frame around them; each route is a full page. This is the common case.\n- A route's `children:` declares sub‑routes that render **inside a `RouterOutlet`** you\n  place in that route's widget — so the parent stays mounted while the body changes.\n  Use this when you want chrome that persists.",
  ),
  DocChunk(
    pagePath: "flutter_modular/nested-routes.md",
    pageTitle: "Nested routes & RouterOutlet",
    heading: "A persistent shell",
    anchor: "a-persistent-shell",
    text: "## A persistent shell\n\nDeclare the shell route with `children`, and render a `RouterOutlet` where the body\ngoes:\n\n```dart\nfinal dashboardModule = createModule(\n  path: '/dashboard',\n  register: (c) {\n    c.route(\n      '/',\n      child: (ctx, state) => const DashboardShell(), // the frame (bottom bar)\n      children: (sub) {\n        sub\n          ..route('/', child: (ctx, state) => const FeedPage())\n          ..route('/search', child: (ctx, state) => const SearchPage())\n          ..route('/profile', child: (ctx, state) => const ProfilePage())\n          ..route('/item', child: (ctx, state) => const ItemPage());\n      },\n    );\n  },\n);\n```\n\nMounted at `/dashboard`, the children resolve to `/dashboard` (Feed), `/dashboard/search`,\n`/dashboard/profile`, `/dashboard/item`. The shell renders the matching child in its\noutlet:\n\n```dart\nclass DashboardShell extends StatefulWidget {\n  const DashboardShell({super.key});\n  @override\n  State<DashboardShell> createState() => _DashboardShellState();\n}\n\nclass _DashboardShellState extends State<DashboardShell> {\n  // The bottom bar is a SIBLING of the outlet, so it drives it through a key\n  // rather than RouterOutlet.of(context).\n  final _outlet = GlobalKey<RouterOutletState>();\n\n  static const _tabs = [\n    '/dashboard',\n    '/dashboard/search',\n    '/dashboard/profile',\n  ];\n\n  @override\n  Widget build(BuildContext context) {\n    // The active tab is DERIVED from the route — never stored. routeState()\n    // rebuilds this when the URL changes, so a deep link lights the right tab.\n    final path = context.routeState().uri.path;\n    final selected = _tabs.lastIndexWhere((t) => path == t || path.startsWith('\$t/'));\n\n    return Scaffold(\n      appBar: AppBar(title: const Text('Dashboard')),\n      body: RouterOutlet(key: _outlet),\n      bottomNavigationBar: NavigationBar(\n        selectedIndex: selected < 0 ? 0 : selected,\n        // A tab tap just navigates the outlet — no local state to flip.\n        onDestinationSelected: (i) => _outlet.currentState?.navigate(_tabs[i]),\n        destinations: const [\n          NavigationDestination(icon: Icon(Icons.dynamic_feed), label: 'Feed'),\n          NavigationDestination(icon: Icon(Icons.search), label: 'Search'),\n          NavigationDestination(icon: Icon(Icons.person), label: 'Profile'),\n        ],\n      ),\n    );\n  }\n}\n```\n\n`RouterOutlet` is a **real nested `Navigator`** with its own push/pop sub‑stack. The\nshell (`DashboardShell`) builds once and stays mounted while the body swaps — its\n`State` is preserved across tab switches.",
  ),
  DocChunk(
    pagePath: "flutter_modular/nested-routes.md",
    pageTitle: "Nested routes & RouterOutlet",
    heading: "Tabs vs. stacking inside an outlet",
    anchor: "tabs-vs-stacking-inside-an-outlet",
    text: "## Tabs vs. stacking inside an outlet\n\nThe two navigation verbs mean different things inside an outlet:\n\n- **`navigate(path)`** *replaces* the outlet's sub‑stack — a **tab switch** (no history\n  to pop back through). That is what the bottom bar calls.\n- **`pushNamed(path)`** *stacks* a page **inside** the same outlet, so the shell\n  persists over it. Pop returns to the tab beneath.\n\nFrom a widget **inside** the outlet you can use the `context` verbs directly — they\ntarget the nearest outlet automatically:\n\n```dart\n// Inside a tab page — stacks /dashboard/item over the current tab, shell intact:\ncontext.pushNamed('/dashboard/item');\n```\n\nThe shell above drives the outlet through a `GlobalKey<RouterOutletState>` instead,\nbecause the bottom bar is a **sibling** of the outlet (not a descendant), so there is no\nenclosing outlet on its `context`.",
  ),
  DocChunk(
    pagePath: "flutter_modular/nested-routes.md",
    pageTitle: "Nested routes & RouterOutlet",
    heading: "Back button on a shell's index page",
    anchor: "back-button-on-a-shell-s-index-page",
    text: "## Back button on a shell's index page\n\nA mounted module's index page is the sole entry of its outlet's nested `Navigator`, so\n`AppBar`'s `automaticallyImplyLeading` cannot see the root stack underneath and shows no\narrow on its own. Use `context.canPop()` to decide whether to show one:\n\n```dart\nAppBar(\n  leading: context.canPop()\n      ? BackButton(onPressed: () => context.pop())\n      : null,\n);\n```",
  ),
  DocChunk(
    pagePath: "flutter_modular/nested-routes.md",
    pageTitle: "Nested routes & RouterOutlet",
    heading: "The URL follows the outlet",
    anchor: "the-url-follows-the-outlet",
    text: "## The URL follows the outlet\n\nSwitching tabs changes the URL (`/dashboard` → `/dashboard/search`) because the outlet\nreports its **base** sub‑route to the root delegate. A `pushNamed` *inside* the outlet\nleaves that base unchanged, so — consistent with the [stack‑base URL\nmodel](./navigation.md#the-url-reflects-the-stack-base) — a stacked page stays out of the\nURL while a tab switch shows up in it.\n\n:::tip One outlet, one job\nReach for `RouterOutlet` only when you want a frame that **persists** across child\nroutes. If each destination is a full page with no shared chrome, plain `module(...)`\nflattening is simpler.\n:::",
  ),
  DocChunk(
    pagePath: "flutter_modular/start.md",
    pageTitle: "Getting started",
    heading: "",
    anchor: "",
    text: "# Getting started\n\nThis page builds the smallest possible Modular app — a counter — and explains each\npiece. By the end you will have a module, an app bootstrap, and a page‑scoped view\nmodel wired to a route.",
  ),
  DocChunk(
    pagePath: "flutter_modular/start.md",
    pageTitle: "Getting started",
    heading: "Install",
    anchor: "install",
    text: "## Install\n\nAdd **flutter_modular** to your project:\n\n```bash\nflutter pub add flutter_modular\n```\n\nThat yields a dependency on the v7 line:\n\n```yaml title=\"pubspec.yaml\"\ndependencies:\n  flutter_modular: ^7.0.0\n```",
  ),
  DocChunk(
    pagePath: "flutter_modular/start.md",
    pageTitle: "Getting started",
    heading: "A module is DI + Routes",
    anchor: "a-module-is-di-routes",
    text: "## A module is DI + Routes\n\nEverything starts with a **Module**: the object that declares a scope's dependency\ninjection and its routes. Build one functionally with `createModule`:\n\n```dart\nimport 'package:flutter_modular/flutter_modular.dart';\n\nfinal appModule = createModule(\n  register: (c) {\n    c.route(\n      '/',\n      provide: (s) => s.addChangeNotifier<CounterViewModel>(CounterViewModel.new),\n      child: (context, state) => const CounterPage(),\n    );\n  },\n);\n```\n\n- `c.route('/', child: ...)` declares the route shown at `/`. The builder receives the\n  `BuildContext` and a [`RouteState`](./navigation.md#routestate) (path params, query,\n  arguments).\n- `provide:` declares **page‑scoped state** for that route — here a `CounterViewModel`\n  built when the page mounts and `dispose()`d when it leaves. More on that in\n  [State management](./state-management.md).\n\n:::tip Store modules in a `final`\nModules are deduplicated **by identity**. Keep each one in a top‑level `final` and\nreference that same value everywhere it is composed — never `createModule(...)` twice\nfor the same logical module.\n:::",
  ),
  DocChunk(
    pagePath: "flutter_modular/start.md",
    pageTitle: "Getting started",
    heading: "Bootstrap with ModularApp",
    anchor: "bootstrap-with-modularapp",
    text: "## Bootstrap with ModularApp\n\n`ModularApp` is the **first widget** of your app, sitting *above* `MaterialApp`. It\nbootstraps the module once (collecting its routes + DI), owns the resulting injector,\nand builds the router config:\n\n```dart title=\"lib/main.dart\"\nimport 'package:flutter/material.dart';\nimport 'package:flutter_modular/flutter_modular.dart';\n\nvoid main() {\n  runApp(\n    ModularApp(\n      module: appModule,\n      child: const AppRoot(),\n    ),\n  );\n}\n```\n\nThe `child` is your `MaterialApp.router`. It reads the router config that `ModularApp`\nbuilt with `ModularApp.routerConfigOf(context)`:\n\n```dart\nclass AppRoot extends StatelessWidget {\n  const AppRoot({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return MaterialApp.router(\n      title: 'My Smart App',\n      routerConfig: ModularApp.routerConfigOf(context),\n    );\n  }\n}\n```\n\n:::warning\n`ModularApp` must be **above** the `MaterialApp.router` that reads\n`routerConfigOf(context)`. That position is also what makes *app‑scoped* state\n(theme, locale, session) possible — see [State management](./state-management.md#app-scoped-state).\n:::",
  ),
  DocChunk(
    pagePath: "flutter_modular/start.md",
    pageTitle: "Getting started",
    heading: "The full counter",
    anchor: "the-full-counter",
    text: "## The full counter\n\nPutting it together — a complete, runnable app (this is the package's own\n[`example`](https://github.com/Flutterando/modular/tree/master/example) in miniature):\n\n```dart title=\"lib/main.dart\"\nimport 'package:flutter/material.dart';\nimport 'package:flutter_modular/flutter_modular.dart';\n\n/// A page-scoped view model (built per page mount, disposed on exit).\nclass CounterViewModel extends ChangeNotifier {\n  int count = 0;\n\n  void increment() {\n    count++;\n    notifyListeners();\n  }\n}\n\nfinal appModule = createModule(\n  register: (c) {\n    c.route(\n      '/',\n      provide: (s) => s.addChangeNotifier<CounterViewModel>(CounterViewModel.new),\n      child: (context, state) => const CounterPage(),\n    );\n  },\n);\n\nvoid main() {\n  runApp(ModularApp(module: appModule, child: const AppRoot()));\n}\n\nclass AppRoot extends StatelessWidget {\n  const AppRoot({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return MaterialApp.router(\n      title: 'flutter_modular counter',\n      routerConfig: ModularApp.routerConfigOf(context),\n    );\n  }\n}\n\nclass CounterPage extends StatelessWidget {\n  const CounterPage({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    final vm = context.watch<CounterViewModel>(); // reactive, page-scoped\n    return Scaffold(\n      appBar: AppBar(title: const Text('Counter')),\n      body: Center(child: Text('count: \${vm.count}')),\n      floatingActionButton: FloatingActionButton(\n        onPressed: context.read<CounterViewModel>().increment,\n        child: const Icon(Icons.add),\n      ),\n    );\n  }\n}\n```",
  ),
  DocChunk(
    pagePath: "flutter_modular/start.md",
    pageTitle: "Getting started",
    heading: "Configuring the root",
    anchor: "configuring-the-root",
    text: "## Configuring the root\n\n`ModularApp` accepts a few options for the root navigator and the entry route:\n\n```dart\nModularApp(\n  module: appModule,\n  initialRoute: '/home',            // first route when the platform reports no deep link\n  navigatorKey: myNavigatorKey,     // imperative access from outside the tree\n  navigatorObservers: [myObserver], // analytics, RouteObserver, …\n  child: const AppRoot(),\n);\n```\n\n- **`initialRoute`** is shown when the platform hands you the bare `/` (app cold‑start\n  with no deep link). A real entry URL — a web refresh on `/products/3`, an app link —\n  overrides it.\n- **`navigatorKey`** lets you reach the root `Navigator` imperatively (e.g. to show a\n  global dialog). A fresh key is created if you omit it.\n- **`navigatorObservers`** are attached to the root navigator.\n\n:::tip Clean URLs on the web\nCall `usePathUrlStrategy()` (from `package:flutter_web_plugins/url_strategy.dart`) at\nthe top of `main()` to get `/products/3` instead of `/#/products/3`. It is a no‑op off\nthe web.\n:::\n\nThat is enough to run a Modular app. Next, learn how to grow it into multiple features\nwith [Modules](./module.md), or jump to [Navigation](./navigation.md).",
  ),
  DocChunk(
    pagePath: "flutter_modular/state-management.md",
    pageTitle: "State management",
    heading: "",
    anchor: "",
    text: "# State management\n\nThis is the architecture Modular pushes. State is **scoped** and has a **deterministic\nlifecycle**: a view model is built when its page mounts and disposed when the page\nleaves the stack. You don't own globals and you don't write `dispose` calls — the\nframework does. The durable truth stays in a repository/service in\n[DI](./dependency-injection.md); view models are disposable projections over it.",
  ),
  DocChunk(
    pagePath: "flutter_modular/state-management.md",
    pageTitle: "State management",
    heading: "Page‑scoped state with `provide`",
    anchor: "page-scoped-state-with-provide",
    text: "## Page‑scoped state with `provide`\n\nDeclare a route's state in its `provide` callback. Each registration becomes a factory\nbuilt in a **page‑local injector** at mount (its own dependencies resolved from the\nmodule injector), provided to the subtree, and disposed at unmount.\n\n```dart\nfinal productsModule = createModule(\n  path: '/products',\n  register: (c) {\n    c\n      ..route(\n        '/',\n        provide: (s) => s.addChangeNotifier<ProductListViewModel>(ProductListViewModel.new),\n        child: (ctx, state) => const ProductListPage(),\n      )\n      ..route(\n        '/:id',\n        provide: (s) {\n          s\n            ..add<RealtimeConnection>(RealtimeConnection.new)\n            ..addChangeNotifier<ProductDetailViewModel>(ProductDetailViewModel.new)\n            ..addStream<int>(_viewersStream);\n        },\n        child: (ctx, state) => ProductDetailPage(id: state['id']!),\n      );\n  },\n);\n```\n\nThe rule is **`addChangeNotifier`** (a reactive view model) and **`addStream`**\n(stream‑backed state); **`add`** registers a plain non‑reactive object. The `Scoped`\nregistrar (`s`) offers:\n\n| Method | For | Reactive? | Disposed on unmount? |\n|---|---|---|---|\n| `addChangeNotifier<T>(ctor)` | a `ChangeNotifier` view model | ✅ via `watch`/`read` | ✅ `dispose()` |\n| `addStream<T>(create)` | stream‑backed state | ✅ as `StreamValue<T>` | ✅ cancels the subscription |\n| `add<T>(ctor)` | a non‑reactive object (socket, use‑case, config) | ❌ | ✅ if it implements `Disposable` |\n\nReactivity and lifecycle are **independent**: a thing can have either, both, or neither.\nFor reactive objects that don't fit the two rules above — a **BLoC**, a **Cubit**, a\ncontroller exposing a `Listenable` — there are two escape hatches,\n[`addStreamable` and `addListenable`](#exceptions-addstreamable-and-addlistenable).",
  ),
  DocChunk(
    pagePath: "flutter_modular/state-management.md",
    pageTitle: "State management",
    heading: "addChangeNotifier — a reactive view model",
    anchor: "addchangenotifier-a-reactive-view-model",
    text: "### addChangeNotifier — a reactive view model\n\nThe bound type is a `ChangeNotifier` (not a bare `Listenable`) precisely so disposal is\nguaranteed. A page‑scoped VM reads the source of truth instead of holding it:\n\n```dart\n/// Page-scoped: 1:1 with the list view. Reads the repository (SSoT), doesn't own truth.\nclass ProductListViewModel extends ChangeNotifier {\n  ProductListViewModel(this._repo);          // repo injected from the module graph\n  final ProductRepository _repo;\n\n  bool loading = true;\n  List<Product> products = const [];\n\n  Future<void> load() async {\n    products = await _repo.getProducts();\n    loading = false;\n    notifyListeners();\n  }\n}\n```",
  ),
  DocChunk(
    pagePath: "flutter_modular/state-management.md",
    pageTitle: "State management",
    heading: "add — a non‑reactive resource",
    anchor: "add-a-non-reactive-resource",
    text: "### add — a non‑reactive resource\n\nFor something that needs lifecycle but no reactivity — a connection, a subscription\nmanager, a use‑case holding a handle. It is built as a per‑page singleton, so a view\nmodel can **inject the same instance**. If it implements `Disposable`, it is\n`dispose()`d on exit — `add` **always** checks for `Disposable`:\n\n```dart\nclass RealtimeConnection implements Disposable {\n  bool isOpen = true;\n\n  @override\n  void dispose() {\n    isOpen = false; // closed when the detail page leaves the stack\n  }\n}\n```\n\n```dart\nclass ProductDetailViewModel extends ChangeNotifier {\n  // The page's RealtimeConnection (same instance) injected alongside the repo.\n  ProductDetailViewModel(this._repo, this._connection);\n  final ProductRepository _repo;\n  final RealtimeConnection _connection;\n  bool get connected => _connection.isOpen;\n}\n```",
  ),
  DocChunk(
    pagePath: "flutter_modular/state-management.md",
    pageTitle: "State management",
    heading: "addStream — stream‑backed state",
    anchor: "addstream-stream-backed-state",
    text: "### addStream — stream‑backed state\n\n`addStream` exposes the latest value of a stream as a `StreamValue<T>`:\n\n```dart\nStream<int> _viewersStream() =>\n    Stream<int>.periodic(const Duration(seconds: 2), (i) => 40 + i);\n\n// ...provide: (s) => s.addStream<int>(_viewersStream)...\n\n// In the page:\nfinal viewers = context.watch<StreamValue<int>>().value; // latest int, or null\n```",
  ),
  DocChunk(
    pagePath: "flutter_modular/state-management.md",
    pageTitle: "State management",
    heading: "Exceptions: addStreamable and addListenable",
    anchor: "exceptions-addstreamable-and-addlistenable",
    text: "## Exceptions: addStreamable and addListenable\n\n`addChangeNotifier` and `addStream` cover the common cases. When an object's reactivity\nlives on a **property** — its `stream`, or a `Listenable` it exposes — and you want to\nexpose the **object itself** (to read its synchronous state and call its methods), reach\nfor these two escape hatches. Each takes a factory, a selector for the reactive source,\nand a (required) dispose callback:\n\n- `addStreamable<T>(ctor, (t) => t.stream, (t) => t.close())` — reactivity is a `Stream`.\n  `context.watch<T>()` returns the object; rebuilds fire on each emission.\n- `addListenable<T>(ctor, (t) => t.someListenable, (t) => t.dispose())` — reactivity is a\n  `Listenable` property.\n\n```dart\n// A controller that is NOT a ChangeNotifier but exposes one:\nclass SearchController {\n  final ValueNotifier<String> query = ValueNotifier('');\n  void dispose() => query.dispose();\n}\n\nprovide: (s) => s.addListenable<SearchController>(\n  SearchController.new,\n  (c) => c.query,        // the rebuild trigger\n  (c) => c.dispose(),    // cleanup on unmount\n);\n```\n\n:::note\nPrefer `addChangeNotifier`/`addStream`. Use `addStreamable`/`addListenable` only when the\nreactive source is a property of the object you want to expose.\n:::",
  ),
  DocChunk(
    pagePath: "flutter_modular/state-management.md",
    pageTitle: "State management",
    heading: "BLoC and Cubit",
    anchor: "bloc-and-cubit",
    text: "## BLoC and Cubit\n\nA **BLoC** or **Cubit** is exactly the streamable case: it exposes a synchronous `state`,\na `stream` of changes, and an async `close()`. Register it with `addStreamable` —\n`context.watch<T>()` returns the **BLoC/Cubit** itself, so you read `state` directly and\nrebuilds are driven by its stream:\n\n```dart\n// CounterCubit is a Cubit from the `bloc` package.\nroute(\n  '/counter',\n  provide: (s) => s.addStreamable<CounterCubit>(\n    CounterCubit.new,\n    (c) => c.stream,\n    (c) => c.close(),\n  ),\n  child: (ctx, state) {\n    final counter = ctx.watch<CounterCubit>(); // the Cubit itself\n    return Text('\${counter.state}');           // read its synchronous state\n  },\n);\n```\n\nflutter_modular has **no dependency on the `bloc` package** — `addStreamable` takes the\n`stream` and `close` as callbacks. To make this a one‑liner, add a small extension on\n`Scoped` in your app. Because both **BLoC** and **Cubit** extend `BlocBase` (which has\n`.stream` and `.close()`), a single `addBloc` covers both:\n\n```dart\nimport 'package:bloc/bloc.dart';\nimport 'package:flutter_modular/flutter_modular.dart';\n\n/// Registers a page-scoped BLoC or Cubit: reactive via its stream, closed on unmount.\nextension BlocScoped on Scoped {\n  void addBloc<B extends BlocBase<Object?>>(B Function() create) =>\n      addStreamable<B>(create, (b) => b.stream, (b) => b.close());\n}\n```\n\n```dart\n// Now registering any BLoC or Cubit is one line:\nprovide: (s) => s.addBloc<CounterCubit>(CounterCubit.new),\n```\n\n:::tip\nWith the extension above, `addBloc<MyBloc>(MyBloc.new)` works for both **BLoC** and\n**Cubit** — one line to get a page‑scoped, auto‑closed, reactive instance.\n:::",
  ),
  DocChunk(
    pagePath: "flutter_modular/state-management.md",
    pageTitle: "State management",
    heading: "Reading state: `watch` and `read`",
    anchor: "reading-state-watch-and-read",
    text: "## Reading state: `watch` and `read`\n\nFrom any descendant of the page, reach a provided `Listenable`:\n\n```dart\nfinal vm = context.watch<ProductListViewModel>(); // rebuilds this widget on notify\ncontext.read<ProductListViewModel>().load();       // reads without subscribing (callbacks)\n```\n\n- `context.watch<T>()` subscribes — the widget rebuilds when `T` notifies.\n- `context.read<T>()` does **not** subscribe — use it in callbacks (`onPressed`) and\n  one‑shot calls.",
  ),
  DocChunk(
    pagePath: "flutter_modular/state-management.md",
    pageTitle: "State management",
    heading: "Granular rebuilds: `Consumer` and `Selector`",
    anchor: "granular-rebuilds-consumer-and-selector",
    text: "## Granular rebuilds: `Consumer` and `Selector`\n\n`watch` rebuilds the whole widget that called it. To scope a rebuild to a sub‑tree, use\n`Consumer`; to rebuild only when a *derived value* changes, use `Selector`:\n\n```dart\n// Rebuilds only this builder when the VM notifies:\nConsumer<CartViewModel>(\n  builder: (context, cart, child) => Text('\${cart.items.length} items'),\n);\n\n// Rebuilds only when the selected value changes:\nSelector<CartViewModel, int>(\n  selector: (context, cart) => cart.items.length,\n  builder: (context, count, child) => Badge(label: Text('\$count')),\n);\n```",
  ),
  DocChunk(
    pagePath: "flutter_modular/state-management.md",
    pageTitle: "State management",
    heading: "App‑scoped state {#app-scoped-state}",
    anchor: "app-scoped-state-app-scoped-state",
    text: "## App‑scoped state {#app-scoped-state}\n\nPage‑scoped state lives *below* the `Navigator`, so it cannot rebuild the `MaterialApp`\nitself. For app‑global state that drives **theme, locale, or session**, declare it on\n`ModularApp.provide` — the very same `Scoped` mechanism, only anchored **above** the\n`MaterialApp`:\n\n```dart\nvoid main() {\n  runApp(\n    ModularApp(\n      module: appModule,\n      provide: (s) => s.addChangeNotifier<ThemeController>(ThemeController.new),\n      child: const AppRoot(),\n    ),\n  );\n}\n\nclass AppRoot extends StatelessWidget {\n  const AppRoot({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    final theme = context.watch<ThemeController>(); // above the MaterialApp\n    return MaterialApp.router(\n      themeMode: theme.mode,\n      theme: ThemeData.light(useMaterial3: true),\n      darkTheme: ThemeData.dark(useMaterial3: true),\n      routerConfig: ModularApp.routerConfigOf(context),\n    );\n  }\n}\n```\n\n```dart\nclass ThemeController extends ChangeNotifier {\n  ThemeMode mode = ThemeMode.light;\n  void toggle() {\n    mode = mode == ThemeMode.light ? ThemeMode.dark : ThemeMode.light;\n    notifyListeners();\n  }\n}\n```\n\nBecause it is anchored above the `Navigator`, a page deep in the tree can still reach it\nwith `context.read<ThemeController>().toggle()` — and the whole app re‑themes.",
  ),
  DocChunk(
    pagePath: "flutter_modular/state-management.md",
    pageTitle: "State management",
    heading: "Disposable {#disposable}",
    anchor: "disposable-disposable",
    text: "## Disposable {#disposable}\n\n`Disposable` is the interface that opts a non‑reactive class into page‑scoped lifecycle:\n\n```dart\nabstract interface class Disposable {\n  void dispose();\n}\n```\n\nImplement it and register with `add` (page‑scoped) — Modular builds it in the\npage‑local injector and calls `dispose()` on unmount. Feature‑module binds that\nimplement `Disposable` (or `ChangeNotifier`) are likewise disposed when the feature\nleaves the stack; see [DI lifecycle](./dependency-injection.md#bind-lifecycle).",
  ),
  DocChunk(
    pagePath: "flutter_modular/state-management.md",
    pageTitle: "State management",
    heading: "Why this is the architecture",
    anchor: "why-this-is-the-architecture",
    text: "## Why this is the architecture\n\n- **One home for the truth.** Repositories/services are root‑owned singletons; a view\n  model reads them and never becomes a competing source of truth.\n- **No floating state.** A VM exists exactly as long as its page; leaving disposes it.\n- **State management gets lighter.** Lifecycle and provision are the framework's job, so\n  whatever reactivity you choose has less to carry.",
  ),
  DocChunk(
    pagePath: "flutter_modular/testing.md",
    pageTitle: "Testing",
    heading: "",
    anchor: "",
    text: "# Testing\n\nBecause a module is just data — routes + DI — you can bootstrap one in a test and assert\non it directly, with no widgets. For UI you mount the router config in a `MaterialApp`\nand pump as usual. The patterns below mirror **flutter_modular**'s own test suite.",
  ),
  DocChunk(
    pagePath: "flutter_modular/testing.md",
    pageTitle: "Testing",
    heading: "Bootstrapping a module",
    anchor: "bootstrapping-a-module",
    text: "## Bootstrapping a module\n\n`bootstrapModule(module)` walks the module tree and returns a `ModularBootstrap` with\nthe matched `routes`, the `injector`, and the lifecycle `manager`:\n\n```dart\nimport 'package:flutter_modular/flutter_modular.dart';\nimport 'package:flutter_test/flutter_test.dart';\n\nclass Counter {\n  int value = 0;\n}\n\nfinal adminModule = createModule(\n  path: '/admin',\n  register: (c) => c.route('/', child: (ctx, state) => const Text('admin home')),\n);\n\nfinal appModule = createModule(\n  register: (c) {\n    c\n      ..addSingleton<Counter>(Counter.new)\n      ..route('/', child: (ctx, state) => const Text('home'))\n      ..route('/product/:id', child: (ctx, state) => Text('product \${state['id']}'))\n      ..module(adminModule);\n  },\n);\n\nvoid main() {\n  test('routes (params + mounted submodule) and binds', () {\n    final boot = bootstrapModule(appModule);\n\n    // Match a Uri to a chain of routes (root → leaf) with captured params.\n    expect(boot.routes.match(Uri.parse('/'))?.last.route.path, '/');\n    expect(boot.routes.match(Uri.parse('/product/42'))?.last.params['id'], '42');\n\n    // A mounted module is a namespace: '/admin' is a single, flattened level.\n    final admin = boot.routes.match(Uri.parse('/admin'));\n    expect(admin?.length, 1);\n    expect(admin?.last.route.path, '/admin');\n\n    // Binds resolve from the graph.\n    expect(boot.injector.get<Counter>(), isA<Counter>());\n  });\n}\n```",
  ),
  DocChunk(
    pagePath: "flutter_modular/testing.md",
    pageTitle: "Testing",
    heading: "Asserting mount‑path rules",
    anchor: "asserting-mount-path-rules",
    text: "### Asserting mount‑path rules\n\nA module's `path` must be a static prefix — a good thing to lock down in a test:\n\n```dart\nexpect(() => createModule(path: '/checkout', register: (_) {}), returnsNormally);\nexpect(() => createModule(register: (_) {}), returnsNormally);            // no path ok\nexpect(() => createModule(path: 'checkout', register: (_) {}),\n    throwsA(isA<AssertionError>()));                                       // must start with '/'\nexpect(() => createModule(path: '/users/:id', register: (_) {}),\n    throwsA(isA<AssertionError>()));                                       // no ':param'\n```",
  ),
  DocChunk(
    pagePath: "flutter_modular/testing.md",
    pageTitle: "Testing",
    heading: "Resolving dependencies",
    anchor: "resolving-dependencies",
    text: "## Resolving dependencies\n\nAfter `bootstrapModule`, `inject<T>()` resolves from the active graph — handy for testing\nservices and the values guards see:\n\n```dart\ntest('inject<T>() resolves from the active graph after bootstrap', () {\n  final module = createModule(register: (c) => c.addSingleton<Service>(Service.new));\n  bootstrapModule(module);\n\n  expect(inject<Service>(), isA<Service>());\n  expect(inject<Service>().hello(), 'hi');\n});\n```",
  ),
  DocChunk(
    pagePath: "flutter_modular/testing.md",
    pageTitle: "Testing",
    heading: "Testing routes in a widget",
    anchor: "testing-routes-in-a-widget",
    text: "## Testing routes in a widget\n\nMount the bootstrapped `routes` through `modularRouterConfig` inside a\n`MaterialApp.router`, then pump. Pass `injector`/`manager` when the routes resolve binds\nor provide page‑scoped state:\n\n```dart\ntestWidgets('renders a route declared by a mounted module', (tester) async {\n  final boot = bootstrapModule(appModule);\n\n  await tester.pumpWidget(\n    MaterialApp.router(\n      routerConfig: modularRouterConfig(boot.routes, initialRoute: '/admin'),\n    ),\n  );\n  await tester.pumpAndSettle();\n\n  expect(find.text('admin home'), findsOneWidget);\n});\n```",
  ),
  DocChunk(
    pagePath: "flutter_modular/testing.md",
    pageTitle: "Testing",
    heading: "Testing guards",
    anchor: "testing-guards",
    text: "## Testing guards\n\nA [guard](./navigation.md#guards) redirects before its page is shown — assert the\ndestination renders instead:\n\n```dart\ntestWidgets('a guard redirects before the page is shown', (tester) async {\n  final boot = bootstrapModule(guardedModule);\n  await tester.pumpWidget(\n    MaterialApp.router(\n      routerConfig: modularRouterConfig(boot.routes, initialRoute: '/admin'),\n    ),\n  );\n  await tester.pumpAndSettle();\n\n  expect(find.text('login'), findsOneWidget); // guard sent us here\n  expect(find.text('admin'), findsNothing);\n});\n```",
  ),
  DocChunk(
    pagePath: "flutter_modular/testing.md",
    pageTitle: "Testing",
    heading: "Testing page‑scoped state",
    anchor: "testing-page-scoped-state",
    text: "## Testing page‑scoped state\n\nA page‑scoped view model is built with its module dependencies at mount and disposed on\nunmount. You can assert both — `watch` rebuilds on `notifyListeners`, and unmounting the\ntree triggers `dispose()`:\n\n```dart\ntestWidgets('builds the VM with deps, rebuilds on notify, disposes on unmount',\n    (tester) async {\n  final boot = bootstrapModule(pModule);\n\n  await tester.pumpWidget(\n    MaterialApp.router(\n      routerConfig: modularRouterConfig(\n        boot.routes,\n        injector: boot.injector,\n        initialRoute: '/product',\n      ),\n    ),\n  );\n  await tester.pumpAndSettle();\n\n  await tester.tap(find.text('load'));     // calls vm.load() via context.read\n  await tester.pumpAndSettle();\n  expect(find.text('Widget X'), findsOneWidget); // dep resolved + watch rebuilt\n\n  await tester.pumpWidget(const SizedBox()); // unmount → VM.dispose() runs\n  await tester.pumpAndSettle();\n});\n```\n\n:::tip\nSee the [`test/`](https://github.com/Flutterando/modular/tree/master/test) directory in\nthe repository for the full suite — routing, guards, `provide`/dispose, outlets,\nrelative routes, arguments, transitions and web URLs.\n:::",
  ),
  DocChunk(
    pagePath: "intro.md",
    pageTitle: "Welcome to Modular",
    heading: "",
    anchor: "",
    text: "# Welcome to Modular\n\n**flutter_modular** gives a Flutter app a *structure*: it turns the two things that\ncouple an application — **Dependency Injection** and **Routes** — into small,\nself‑contained **Modules**, and ties application **state to the route lifecycle** so\nthat ownership and disposal stop being your problem.\n\n:::tip flutter_modular 7 is a ground‑up rewrite\nThe API on this page (and the whole **flutter_modular** section) describes **v7**. If\nyou are still on v6 or v5, see **(Legacy) Modular 6** / **(Legacy) Modular 5** in the\nsidebar, and the [migration guide](./flutter_modular/migration.md) when you are ready\nto upgrade.\n:::",
  ),
  DocChunk(
    pagePath: "intro.md",
    pageTitle: "Welcome to Modular",
    heading: "What is Modular?",
    anchor: "what-is-modular",
    text: "## What is Modular?\n\nIn a monolithic architecture the whole app is a single module. That is quick and\nelegant for small apps, but a larger app built that way accrues technical debt in both\nmaintenance and scalability. By dividing the app **by scope** instead, we gain:\n\n- Improved understanding of features.\n- Fewer breaking changes.\n- New, non‑conflicting features added in isolation.\n- Fewer blind spots in the main business rules.\n- Easier developer turnover.\n\nCompare a typical MVC, where every layer is global:",
  ),
  DocChunk(
    pagePath: "intro.md",
    pageTitle: "Welcome to Modular",
    heading: "A typical MVC",
    anchor: "a-typical-mvc",
    text: "### A typical MVC\n\n    .\n    ├── models                                  # All models\n    │   ├── auth_model.dart\n    │   ├── home_model.dart\n    │   └── product_model.dart\n    ├── controller                              # All controllers\n    │   ├── auth_controller.dart\n    │   ├── home_controller.dart\n    │   └── product_controller.dart\n    ├── views                                   # All views\n    │   ├── auth_page.dart\n    │   ├── home_page.dart\n    │   └── product_page.dart\n    ├── core                                    # Tools and utilities\n    ├── app_widget.dart                         # Main Widget containing MaterialApp\n    └── main.dart                               # runApp\n\n…with the same app divided by scope, where each feature owns its own MVC:",
  ),
  DocChunk(
    pagePath: "intro.md",
    pageTitle: "Welcome to Modular",
    heading: "Structure divided by scope",
    anchor: "structure-divided-by-scope",
    text: "### Structure divided by scope\n\n    .\n    ├── features                                 # All features / Modules\n    │   ├─ auth                                  # Auth's MVC\n    │   │  ├── auth_model.dart\n    │   │  ├── auth_controller.dart\n    │   │  └── auth_page.dart\n    │   ├─ home                                  # Home's MVC\n    │   │  ├── home_model.dart\n    │   │  ├── home_controller.dart\n    │   │  └── home_page.dart\n    │   └─ product                               # Product's MVC\n    │      ├── product_model.dart\n    │      ├── product_controller.dart\n    │      └── product_page.dart\n    ├── core                                     # Tools and utilities\n    ├── app_widget.dart                          # Main Widget containing MaterialApp\n    └── main.dart                                # runApp\n\nWe call this **Smart Structure**: each feature keeps its own MVC, which alone solves\nmany scalability and maintainability problems. But two things stay *global* and fight\nthe structure — **routes** and **dependency injection**. Modular exists to scope\nexactly those two. We group a feature's routes and injections into an object and call\nit a **Module**.",
  ),
  DocChunk(
    pagePath: "intro.md",
    pageTitle: "Welcome to Modular",
    heading: "The architecture Modular pushes",
    anchor: "the-architecture-modular-pushes",
    text: "## The architecture Modular pushes\n\nModular is opinionated on purpose. A few ideas run through the whole package:\n\n- **A Module is DI + Routes.** Those are the two coupling factors in a Flutter app, and\n  a [Module](./flutter_modular/module.md) makes exactly those visible. A module mounted\n  at a `path` is a *feature*; a module without a path is *shared DI*.\n\n- **State is page‑scoped, with a deterministic lifecycle.** A view model declared with\n  [`provide`](./flutter_modular/state-management.md) is built when its page mounts and\n  `dispose()`d when the page leaves the stack. Lifecycle becomes the framework's job —\n  which is what lets Modular take weight off whatever \"state management\" you reach for.\n\n- **The Single Source of Truth lives in a repository/service.** Durable truth is a\n  root‑owned singleton registered in [DI](./flutter_modular/dependency-injection.md);\n  view models are disposable, per‑page projections over it. They never *hold* the truth.\n\n- **Routing reads like the web.** The URL mirrors the navigation **stack base**;\n  `pushNamed` stacks a page that stays out of the URL (modal‑like); routes can be\n  **relative** to where you are, resolved like directories. See\n  [Navigation](./flutter_modular/navigation.md).",
  ),
  DocChunk(
    pagePath: "intro.md",
    pageTitle: "Welcome to Modular",
    heading: "Ready to get started?",
    anchor: "ready-to-get-started",
    text: "## Ready to get started?\n\nModular does something powerful — componentizing routes and dependency injection — and\ndoes it simply. Head to [Getting started](./flutter_modular/start.md) and begin your\njourney toward a smart structure.",
  ),
  DocChunk(
    pagePath: "intro.md",
    pageTitle: "Welcome to Modular",
    heading: "Common questions",
    anchor: "common-questions",
    text: "## Common questions\n\n- **Does Modular work with any state‑management approach?**\n  Yes. The DI system is agnostic to the class it builds, including any reactivity. On\n  top of that, Modular ships its own page‑scoped state (`ChangeNotifier`, streams,\n  `Consumer`/`Selector`) so for many apps you need nothing else.\n\n- **Can I use dynamic routes?**\n  Yes — the route tree matches like the web: dynamic `:params`, query strings, relative\n  paths, nested routes and persistent shells (`RouterOutlet`) are all supported. Use a\n  route [guard](./flutter_modular/navigation.md#guards) to redirect (e.g. to a 404 or a\n  login page).\n\n- **Do I need a Module for every feature?**\n  No. Create a module when a feature earns its own boundary — its own routes and\n  injections — or keep small things in a parent module.\n\n- **I'm writing pure Dart, not Flutter.**\n  Use [`auto_injector`](https://pub.dev/packages/auto_injector) directly — it is the DI\n  engine **flutter_modular** is built on.",
  ),
];
